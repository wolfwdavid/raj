---
phase: 01-deploy-pipeline-walking-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/deploy.yml
  - scripts/smoke-deploy.sh
autonomous: true
requirements: [INFRA-01]
must_haves:
  truths:
    - "The build job builds all four sites in a fail-fast:false matrix, each with BASE_PATH=/<repo>/<site>"
    - "The assemble job downloads all four plain artifacts, un-nests them into _site/<site>/, and fails loudly unless exactly four <site>/index.html exist"
    - "Exactly one Pages deployment is produced (single upload-pages-artifact + single deploy-pages), never per matrix leg"
    - "The GitHub Pages first-enable race is documented in the workflow header as an expected manual checkpoint"
    - "A committed smoke script curls the hub, four subpaths (expect 200), and a bad URL (expect 404)"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "matrix build -> assemble -> single deploy pipeline"
      contains: "matrix"
      min_lines: 60
    - path: "scripts/smoke-deploy.sh"
      provides: "post-deploy live HTTP smoke over hub + 4 subpaths + 404"
      contains: "curl"
  key_links:
    - from: "build job upload-artifact (name: site-${{ matrix.site }})"
      to: "assemble job download-artifact (pattern: site-*)"
      via: "artifact name/pattern match"
      pattern: "site-\\$\\{\\{ matrix.site \\}\\}"
    - from: "assemble job cp from _artifacts/site-$site/."
      to: "_site/$site/ (un-nesting download-artifact@v4)"
      via: "explicit nested-path copy + per-site index.html assertion"
      pattern: "_artifacts/site-\\$site"
    - from: "assemble upload-pages-artifact@v3 (path: _site)"
      to: "deploy job deploy-pages@v4"
      via: "single Pages artifact -> single deployment"
      pattern: "upload-pages-artifact"
---

<objective>
Author the single-source-of-truth deploy pipeline: one GitHub Actions workflow that builds all four SvelteKit sites in a parallel matrix, assembles their outputs into one `_site/` tree, and performs exactly one GitHub Pages deployment. Also commit the post-deploy live smoke script that verifies the six URLs after enablement.

Purpose: The matrix->assemble->single-deploy shape is the ONLY novel architectural element of this project and the biggest "looks-done-but-isn't" risk (GitHub allows exactly one Pages deployment per repo — you cannot call `upload-pages-artifact` per matrix leg). Proving it now against trivial content means every later phase rides an unchanged pipeline.
Output: `.github/workflows/deploy.yml` (matrix build + assemble assertions + single deploy) and `scripts/smoke-deploy.sh` (live HTTP gate).

Requirements addressed: INFRA-01 (four sites reachable at `/raj/<site>/` from one deployment after a single push to main).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-deploy-pipeline-walking-skeleton/01-CONTEXT.md
@.planning/phases/01-deploy-pipeline-walking-skeleton/01-RESEARCH.md
@.planning/phases/01-deploy-pipeline-walking-skeleton/01-VALIDATION.md

Locked decisions this plan implements: D-07 (build matrix, fail-fast:false, per-site npm cache, BASE_PATH, plain upload-artifact named `site-<name>`), D-08 (assemble job: download-artifact `site-*`, un-nest, copy hub/404/.nojekyll, assert exactly four `<site>/index.html`), D-09 (deploy job: deploy-pages@v4, github-pages env, concurrency group pages cancel-in-progress, workflow_dispatch), D-10 (configure-pages@v5 enablement:true + first-enable race documented as manual checkpoint).

<interfaces>
<!-- Filenames/paths the workflow references — these are CREATED by plan 01-02 (same wave). -->
<!-- The workflow does NOT need them to exist at author time; it consumes them only in CI. -->
Site folders the build matrix iterates: vfamigos, lipool, lidentist, cannaworldnews
  Each provides: <site>/package-lock.json (for npm cache + npm ci) and <site>/build/ (adapter-static output, contains index.html + _app/)
Repo-infra folder the assemble job checks out and copies from:
  pages-root/index.html, pages-root/404.html, pages-root/.nojekyll
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author deploy.yml (matrix build -> assemble -> single deploy)</name>
  <files>.github/workflows/deploy.yml</files>
  <read_first>
    - .github/workflows/deploy.yml (if it already exists — do not blindly overwrite; this plan creates it fresh if absent)
    - .planning/phases/01-deploy-pipeline-walking-skeleton/01-RESEARCH.md (Code Examples section has the full verbatim YAML; Pitfalls 1-3 explain first-enable race, download-artifact@v4 nesting, include-hidden-files default-false)
    - raj_one/.github/workflows/deploy.yml (single-site ancestor — for reference on action versions only; do NOT copy its single-site upload-pages-artifact-in-build shape)
    - .gitignore (confirms build/, node_modules/, .svelte-kit/ are ignored; site folders are NOT ignored)
  </read_first>
  <action>
Create `.github/workflows/deploy.yml` with EXACTLY the following content (copied verbatim from 01-RESEARCH.md Code Examples — every value is load-bearing):

```yaml
# .github/workflows/deploy.yml
# FIRST DEPLOY: enablement:true below often no-ops on a brand-new repo (Pages first-enable race).
# If the first green run 404s, set Settings -> Pages -> Source = "GitHub Actions" by hand,
# then re-run this workflow via the "Run workflow" button (workflow_dispatch).
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false                       # surface all four sites' status every run
      matrix:
        site: [vfamigos, lipool, lidentist, cannaworldnews]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: ${{ matrix.site }}/package-lock.json
      - run: npm ci
        working-directory: ${{ matrix.site }}
      - run: npm run build
        working-directory: ${{ matrix.site }}
        env:
          BASE_PATH: /${{ github.event.repository.name }}/${{ matrix.site }}   # e.g. /raj/vfamigos
      - uses: actions/upload-artifact@v4       # plain artifact — NOT upload-pages-artifact
        with:
          name: site-${{ matrix.site }}
          path: ${{ matrix.site }}/build
          if-no-files-found: error
          include-hidden-files: true

  assemble:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4              # for pages-root/
      - uses: actions/download-artifact@v4     # nests each under _artifacts/site-<name>/
        with:
          path: _artifacts
          pattern: site-*
      - name: Assemble _site
        run: |
          set -euo pipefail
          mkdir -p _site
          cp pages-root/index.html _site/index.html
          cp pages-root/404.html   _site/404.html
          cp pages-root/.nojekyll  _site/.nojekyll
          for site in vfamigos lipool lidentist cannaworldnews; do
            test -d "_artifacts/site-$site" || { echo "::error::missing artifact for $site"; exit 1; }
            mkdir -p "_site/$site"
            cp -r "_artifacts/site-$site/." "_site/$site/"
            test -f "_site/$site/index.html" || { echo "::error::missing index.html for $site"; exit 1; }
          done
          count=$(ls -1 _site/*/index.html | wc -l)
          test "$count" -eq 4 || { echo "::error::expected 4 site index.html, got $count"; exit 1; }
      - uses: actions/configure-pages@v5
        with:
          enablement: true                     # idempotent; may no-op on first run (see header)
      - uses: actions/upload-pages-artifact@v3 # the ONE Pages artifact
        with:
          path: _site

  deploy:
    needs: assemble
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
```

Do NOT alter any of these load-bearing details:
- `fail-fast: false` (surfaces all four sites' status even if one fails).
- Build-leg upload uses plain `actions/upload-artifact@v4` with `name: site-${{ matrix.site }}`, `include-hidden-files: true`, `if-no-files-found: error` — NEVER `upload-pages-artifact` inside the matrix.
- `BASE_PATH: /${{ github.event.repository.name }}/${{ matrix.site }}` — repo name flows from context, zero hardcoded `/raj/`.
- Assemble copies from the NESTED path `_artifacts/site-$site/.` (download-artifact@v4 nests one dir deeper) and asserts per-site `index.html` plus a total count of 4.
- Exactly ONE `upload-pages-artifact@v3` (in assemble) and ONE `deploy-pages@v4` (in deploy).
- `concurrency: {group: pages, cancel-in-progress: true}` and `workflow_dispatch` present.
- The header comment documenting the first-enable manual checkpoint (D-10) is retained verbatim.
  </action>
  <verify>
    <automated>python -c "import yaml; d=yaml.safe_load(open('.github/workflows/deploy.yml')); assert set(d['jobs'])=={'build','assemble','deploy'}; assert d['jobs']['build']['strategy']['fail-fast'] is False; assert d['jobs']['build']['strategy']['matrix']['site']==['vfamigos','lipool','lidentist','cannaworldnews']; print('deploy.yml OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml'))"` exits 0 (valid YAML).
    - `grep -c "upload-pages-artifact" .github/workflows/deploy.yml` returns exactly `1` (single Pages artifact, not per-matrix-leg).
    - `grep -c "deploy-pages@v4" .github/workflows/deploy.yml` returns exactly `1`.
    - `grep -q "fail-fast: false" .github/workflows/deploy.yml` succeeds.
    - `grep -q "name: site-\${{ matrix.site }}" .github/workflows/deploy.yml` succeeds (plain artifact naming).
    - `grep -q 'BASE_PATH: /\${{ github.event.repository.name }}/\${{ matrix.site }}' .github/workflows/deploy.yml` succeeds (no hardcoded /raj/).
    - `grep -q '_artifacts/site-\$site/\.' .github/workflows/deploy.yml` succeeds (nested-path copy handling download-artifact@v4).
    - `grep -q 'expected 4 site index.html' .github/workflows/deploy.yml` succeeds (count==4 assertion present).
    - `grep -q "cancel-in-progress: true" .github/workflows/deploy.yml` succeeds.
    - `grep -q "workflow_dispatch" .github/workflows/deploy.yml` succeeds.
    - `grep -qi "first-enable\|Settings -> Pages" .github/workflows/deploy.yml` succeeds (first-enable checkpoint documented).
    - `grep -qi "include-hidden-files: true" .github/workflows/deploy.yml` succeeds.
  </acceptance_criteria>
  <done>deploy.yml exists at .github/workflows/deploy.yml, is valid YAML, has exactly three jobs (build matrix / assemble / deploy), contains exactly one upload-pages-artifact and one deploy-pages, un-nests download-artifact@v4 output, asserts exactly four site index.html files, and documents the first-enable manual checkpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the post-deploy live smoke script</name>
  <files>scripts/smoke-deploy.sh</files>
  <read_first>
    - .planning/phases/01-deploy-pipeline-walking-skeleton/01-RESEARCH.md (Validation Architecture section — the six-URL smoke loop)
    - .planning/phases/01-deploy-pipeline-walking-skeleton/01-VALIDATION.md (phase gate: full builds green AND Actions green AND live URLs 200)
  </read_first>
  <action>
Create `scripts/smoke-deploy.sh` with EXACTLY this content. This is the phase-gate instrument that verifies INFRA-01/02/03 against the live host AFTER the first-enable manual step. It defaults to the raj deployment base but accepts an override argument (forward-compatible with custom-domain cutover):

```bash
#!/usr/bin/env bash
# Post-deploy live smoke for the raj monorepo GitHub Pages deployment.
# Run AFTER the first-enable manual step (Settings -> Pages -> Source = GitHub Actions)
# and a green Actions run. Usage: scripts/smoke-deploy.sh [BASE_URL]
set -euo pipefail

BASE="${1:-https://wolfwdavid.github.io/raj}"
fail=0

echo "== Smoke test: $BASE =="
for p in "" vfamigos/ lipool/ lidentist/ cannaworldnews/; do
  code=$(curl -s -o /dev/null -w '%{http_code}' "$BASE/$p")
  printf '%s  %s\n' "$code" "$BASE/$p"
  [ "$code" = "200" ] || { echo "::error::expected 200 for $BASE/$p"; fail=1; }
done

# Any unknown URL must serve the styled root 404.
badurl="$BASE/nonexistent-$(date +%s)/"
code=$(curl -s -o /dev/null -w '%{http_code}' "$badurl")
printf '%s  %s (expect 404)\n' "$code" "$badurl"
[ "$code" = "404" ] || { echo "::error::expected 404 for $badurl"; fail=1; }

if [ "$fail" -eq 0 ]; then echo "ALL SMOKE CHECKS PASSED"; else echo "SMOKE FAILURES ABOVE"; fi
exit $fail
```

Make it executable: `chmod +x scripts/smoke-deploy.sh` (also `git update-index --chmod=+x scripts/smoke-deploy.sh` if committing on Windows so the exec bit persists in the repo).
  </action>
  <verify>
    <automated>bash -n scripts/smoke-deploy.sh && echo "smoke script syntax OK"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/smoke-deploy.sh` exits 0 (valid bash syntax).
    - `grep -q 'vfamigos/ lipool/ lidentist/ cannaworldnews/' scripts/smoke-deploy.sh` succeeds (all four subpaths smoked).
    - `grep -q "expect 404" scripts/smoke-deploy.sh` succeeds (bad-URL 404 check present).
    - `grep -q 'https://wolfwdavid.github.io/raj' scripts/smoke-deploy.sh` succeeds (default base) AND `grep -q '\${1:-' scripts/smoke-deploy.sh` succeeds (override arg for custom-domain forward-compat).
    - `grep -q 'exit \$fail' scripts/smoke-deploy.sh` succeeds (non-zero exit on any failure — usable as an automated gate).
  </acceptance_criteria>
  <done>scripts/smoke-deploy.sh exists, is syntactically valid bash, curls the hub + four subpaths expecting 200 and a bad URL expecting 404, exits non-zero on any failure, and accepts an optional base-URL override.</done>
</task>

</tasks>

<verification>
Local (author-time) verification — both automated commands above must pass:
- `python -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml'))"` -> exit 0
- `bash -n scripts/smoke-deploy.sh` -> exit 0
- All grep acceptance criteria in both tasks succeed.

Phase-gate verification (deferred to phase completion, after plan 01-02 also lands and the branch is pushed to main):
- Push to main triggers the workflow; the build matrix runs four legs (fail-fast:false); the assemble job's built-in assertions (four `<site>/index.html`, count==4) gate the single deploy.
- MANUAL CHECKPOINT (Pitfall 1 / D-10): if the first green run 404s or `configure-pages` no-ops, set Settings -> Pages -> Source = "GitHub Actions" by hand, then re-run via `workflow_dispatch`. This is expected on a brand-new repo, not a bug.
- Run `bash scripts/smoke-deploy.sh` -> all six checks green (hub + four subpaths 200, bad URL 404) before `/gsd:verify-work`.
</verification>

<success_criteria>
- `.github/workflows/deploy.yml` exists: valid YAML, three jobs, fail-fast:false matrix over the four sites, plain per-leg `upload-artifact@v4` named `site-<site>`, assemble job un-nests download-artifact@v4 output and asserts exactly four site index.html, exactly one `upload-pages-artifact@v3` + one `deploy-pages@v4`, concurrency + workflow_dispatch + first-enable header comment present.
- `scripts/smoke-deploy.sh` exists, valid bash, smokes the six URLs with correct expected status codes, exits non-zero on failure.
- Zero hardcoded `/raj/` in the workflow (BASE_PATH derives from `github.event.repository.name` + `matrix.site`).
</success_criteria>

<output>
After completion, create `.planning/phases/01-deploy-pipeline-walking-skeleton/01-01-SUMMARY.md`
</output>
