---
phase: 01-deploy-pipeline-walking-skeleton
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - vfamigos/package.json
  - vfamigos/package-lock.json
  - vfamigos/vite.config.ts
  - vfamigos/tsconfig.json
  - vfamigos/.npmrc
  - vfamigos/.gitignore
  - vfamigos/static/favicon.svg
  - vfamigos/src/app.html
  - vfamigos/src/app.d.ts
  - vfamigos/src/routes/+layout.ts
  - vfamigos/src/routes/+layout.svelte
  - vfamigos/src/routes/+page.svelte
  - lipool/
  - lidentist/
  - cannaworldnews/
  - pages-root/index.html
  - pages-root/404.html
  - pages-root/.nojekyll
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-03]
must_haves:
  truths:
    - "Each of the four site folders is a minimal real SvelteKit app that builds with `npm ci && npm run build` and emits build/index.html containing the site's placeholder text"
    - "Each build succeeds with BASE_PATH set (e.g. /raj/vfamigos) — paths.base flows from process.env.BASE_PATH"
    - "Every +layout.ts sets prerender=true and trailingSlash='always' and NEVER ssr=false"
    - "A committed package-lock.json exists per site so CI `npm ci` works"
    - "pages-root/index.html is a styled hub linking all four sites via relative ./<site>/ links (no root-absolute hrefs)"
    - "pages-root/404.html is styled, says page not found, and links the four sites + hub, robust from any depth via <base href='/raj/'>"
    - "pages-root/.nojekyll exists (empty)"
  artifacts:
    - path: "vfamigos/vite.config.ts"
      provides: "inline sveltekit() config with adapter-static + paths.base from BASE_PATH + runes"
      contains: "process.env.BASE_PATH"
    - path: "vfamigos/src/routes/+layout.ts"
      provides: "prerender contract"
      contains: "prerender = true"
    - path: "vfamigos/src/routes/+page.svelte"
      provides: "placeholder page with relative back-to-hub link"
      contains: "coming soon"
    - path: "vfamigos/package-lock.json"
      provides: "committed lockfile for npm ci"
    - path: "pages-root/index.html"
      provides: "hub at /raj/ linking four sites relatively"
      contains: "./vfamigos/"
      min_lines: 20
    - path: "pages-root/404.html"
      provides: "styled root 404 with base href, links four sites + hub"
      contains: "<base href=\"/raj/\">"
      min_lines: 20
    - path: "pages-root/.nojekyll"
      provides: "belt-and-suspenders Jekyll opt-out"
  key_links:
    - from: "vite.config.ts paths.base"
      to: "process.env.BASE_PATH (injected by deploy.yml build leg)"
      via: "(process.env.BASE_PATH ?? '')"
      pattern: "process.env.BASE_PATH"
    - from: "src/routes/+page.svelte back-to-hub link"
      to: "/raj/ hub (one level up from /raj/<site>/)"
      via: "relative href '../' (never {base}-prefixed, never root-absolute)"
      pattern: "href=\"\\.\\./\""
    - from: "pages-root/index.html link cards"
      to: "four site subpaths"
      via: "relative ./<site>/ links (index always served at /raj/)"
      pattern: "\\./(vfamigos|lipool|lidentist|cannaworldnews)/"
    - from: "pages-root/404.html <base href='/raj/'> + relative body links"
      to: "four site subpaths + hub, from any requested depth"
      via: "base tag makes vfamigos/ resolve to /raj/vfamigos/ regardless of bad-URL depth"
      pattern: "<base href=\"/raj/\">"
---

<objective>
Hand-author the four minimal real SvelteKit placeholder apps (vfamigos, lipool, lidentist, cannaworldnews) and the committed static hub + 404 that tie the deployment together. Each site is a genuine `npm ci`/`BASE_PATH`-buildable app (not hand-copied HTML) so the pipeline is proven against real builds; the hub and 404 are dependency-free static pages copied straight into the deployment root.

Purpose: Provides the four buildable subpaths the deploy pipeline (01-01) assembles, plus the hub (INFRA-02) and styled 404 (INFRA-03). Choosing the scaffold correctly now (no ssr=false, prerender contract, relative links, BASE_PATH-driven base) means Phases 2-3 add content without reworking the shell.
Output: four SvelteKit scaffolds with committed lockfiles + `pages-root/{index.html,404.html,.nojekyll}`.

Requirements addressed: INFRA-01 (four buildable sites reachable via the deploy), INFRA-02 (hub), INFRA-03 (styled root 404).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-deploy-pipeline-walking-skeleton/01-CONTEXT.md
@.planning/phases/01-deploy-pipeline-walking-skeleton/01-RESEARCH.md

Locked decisions this plan implements: D-01 (four minimal real SvelteKit apps, raj_one toolchain minus adapter-auto minus ssr=false, own package.json/lockfile), D-02 (placeholder = site name + "coming soon" + link back to hub, no design-system styling), D-03 (+layout.ts sets prerender=true + trailingSlash='always', never ssr=false), D-04 (hub is committed static HTML, wordmark + four relative link cards), D-05 (404 shares hub look, links four sites + hub), D-06 (.nojekyll in artifact root).

Planner decisions on research Open Questions (sign-off recorded here):
- OQ1 (404 relative-link fragility): ADOPT `<base href="/raj/">` in 404.html <head>, keep body links relative to that base. This hardcodes `/raj/` ONLY in the 404 — acceptable because pages-root/ is repo infrastructure that never extracts to a custom domain and is regenerated/replaced at any future cutover. The hub index.html gets NO <base> tag (it is only ever served at /raj/).
- OQ2 (static/favicon.svg): INCLUDE per site (tiny, forward-compatible), referenced via `%sveltekit.assets%/favicon.svg` in app.html (base-path-safe, never root-absolute).
- OQ3 (@types/node ^25 vs Node 22 runtime): KEEP `^25.9.3` for fleet convention — no functional impact (only process.env is touched).

<interfaces>
<!-- Verbatim reference content extracted from raj_one (gitignored sibling). Copy these EXACTLY per site. -->
<!-- Executor: do NOT explore raj_one during execution — all needed content is inlined in the task actions below. -->
Site folder names (order matters for the deploy matrix): vfamigos, lipool, lidentist, cannaworldnews
raj_one/tsconfig.json, raj_one/src/app.d.ts, raj_one/.npmrc — copied verbatim into every site (content inlined in Task 1).
raj_one/vite.config.ts pattern — inline sveltekit({...}) config, paths.base from BASE_PATH (content inlined in Task 1).
Consumer: deploy.yml (plan 01-01) runs `npm ci` (needs package-lock.json) then `npm run build` with BASE_PATH=/raj/<site>, uploading <site>/build as artifact `site-<site>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the four SvelteKit placeholder apps + generate committed lockfiles</name>
  <files>vfamigos/, lipool/, lidentist/, cannaworldnews/ (each: package.json, package-lock.json, vite.config.ts, tsconfig.json, .npmrc, .gitignore, static/favicon.svg, src/app.html, src/app.d.ts, src/routes/+layout.ts, src/routes/+layout.svelte, src/routes/+page.svelte)</files>
  <read_first>
    - .planning/phases/01-deploy-pipeline-walking-skeleton/01-RESEARCH.md (Per-site minimal file set + Code Examples: vite.config.ts, +layout.ts, +layout.svelte, +page.svelte, package.json; Pitfalls 4-6: ssr=false trap, trailingSlash, npm ci lockfile)
    - .gitignore (root) — confirms node_modules/, build/, .svelte-kit/ ignored (unanchored, covers site subfolders); confirms the new site folders do NOT match raj_*/ and WILL commit
    - .gitattributes — `* text=auto eol=lf` normalizes lockfiles to LF (no Windows/CI churn)
  </read_first>
  <action>
Create four sibling folders at the repo root: `vfamigos/`, `lipool/`, `lidentist/`, `cannaworldnews/`. These are plain tracked directories — NEVER run `git init` inside them (they must NOT become nested repos, and they do NOT match the `raj_*/` gitignore pattern, so they commit normally).

In EACH of the four folders, author the following files. Every file is IDENTICAL across the four sites EXCEPT `package.json` (the `name` field) and `src/routes/+page.svelte` (the `<h1>` and one-liner). Concrete content:

--- `package.json` (change only `"name"` to the folder name — shown for vfamigos) ---
```json
{
  "name": "vfamigos",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "prepare": "svelte-kit sync || echo ''",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.10",
    "@sveltejs/kit": "^2.63.0",
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "@types/node": "^25.9.3",
    "svelte": "^5.56.1",
    "svelte-check": "^4.6.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  }
}
```
(For the other three: `"name": "lipool"`, `"lidentist"`, `"cannaworldnews"`. Do NOT add `@sveltejs/adapter-auto` — it is deliberately dropped.)

--- `vite.config.ts` (identical all four) ---
```ts
import adapter from '@sveltejs/adapter-static';
import { sveltekit } from '@sveltejs/kit/vite';
import { defineConfig } from 'vite';

export default defineConfig({
  plugins: [
    sveltekit({
      compilerOptions: {
        runes: ({ filename }) =>
          filename.split(/[/\\]/).includes('node_modules') ? undefined : true
      },
      adapter: adapter(),
      // '/raj/<site>' when built by the Pages workflow, '' for local dev/preview + custom domain
      paths: { base: (process.env.BASE_PATH ?? '') as '' | `/${string}` }
    })
  ]
});
```

--- `tsconfig.json` (identical all four — verbatim from raj_one) ---
```json
{
	"extends": "./.svelte-kit/tsconfig.json",
	"compilerOptions": {
		"rewriteRelativeImportExtensions": true,
		"allowJs": true,
		"checkJs": true,
		"esModuleInterop": true,
		"forceConsistentCasingInFileNames": true,
		"resolveJsonModule": true,
		"skipLibCheck": true,
		"sourceMap": true,
		"strict": true,
		"moduleResolution": "bundler"
	}
}
```

--- `.npmrc` (identical all four) ---
```
engine-strict=true
```

--- `.gitignore` (identical all four — aids future per-domain extraction) ---
```
node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
```

--- `static/favicon.svg` (identical all four — minimal dependency-free placeholder) ---
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32"><rect width="32" height="32" rx="6" fill="#1a1a1a"/><circle cx="16" cy="16" r="7" fill="#e2e2e2"/></svg>
```

--- `src/app.html` (identical all four — raj_one's, STRIPPED of the two Google-Fonts <link> lines and the dashboard <meta description>, with a base-path-safe favicon link added) ---
```html
<!doctype html>
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" href="%sveltekit.assets%/favicon.svg" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		%sveltekit.head%
	</head>
	<body data-sveltekit-preload-data="hover">
		<div style="display: contents">%sveltekit.body%</div>
	</body>
</html>
```

--- `src/app.d.ts` (identical all four — verbatim from raj_one) ---
```ts
// See https://svelte.dev/docs/kit/types#app.d.ts
// for information about these interfaces
declare global {
	namespace App {
		// interface Error {}
		// interface Locals {}
		// interface PageData {}
		// interface PageState {}
		// interface Platform {}
	}
}

export {};
```

--- `src/routes/+layout.ts` (identical all four — the prerender contract; NEVER ssr=false) ---
```ts
export const prerender = true;
export const trailingSlash = 'always';
// NEVER: export const ssr = false;  (raj_one's dashboard sets it; it ships empty HTML — fatal for SEO later)
```

--- `src/routes/+layout.svelte` (identical all four — minimal shell, no asset import) ---
```svelte
<script lang="ts">
  let { children } = $props();
</script>

{@render children()}
```

--- `src/routes/+page.svelte` (change only the `<h1>` text — shown for vfamigos; D-02 copy is discretionary but keep it minimal, no design-system styling) ---
```svelte
<script lang="ts">
  // "back to hub" is relative — resolves to /raj/ from /raj/<site>/, no hardcoded /raj/
</script>

<main>
  <h1>vfamigos</h1>
  <p>Coming soon.</p>
  <a href="../">&larr; raj hub</a>
</main>
```
(For the other three, set `<h1>lipool</h1>`, `<h1>lidentist</h1>`, `<h1>cannaworldnews</h1>`. The back link stays `href="../"` — relative, never `{base}`-prefixed, never root-absolute — so it survives both the Pages subpath and the future portability grep.)

After authoring all files in all four folders, generate the committed lockfiles by running `npm install` inside each folder (this resolves the `^` ranges and writes `package-lock.json`, which CI's `npm ci` requires). From the repo root in PowerShell:
```powershell
foreach ($s in "vfamigos","lipool","lidentist","cannaworldnews") {
  Push-Location $s; npm install; Pop-Location
}
```
(Bash equivalent: `for s in vfamigos lipool lidentist cannaworldnews; do (cd "$s" && npm install); done`.)

Then verify each site builds with the Pages BASE_PATH and emits real HTML. Per site (example vfamigos):
```bash
cd vfamigos && npm ci
BASE_PATH=/raj/vfamigos npm run build
test -f build/index.html && grep -q vfamigos build/index.html && test -d build/_app && echo OK
cd ..
```
Do NOT commit `node_modules/`, `build/`, or `.svelte-kit/` (root .gitignore already excludes them). DO commit each site's `package-lock.json`.
  </action>
  <verify>
    <automated>for s in vfamigos lipool lidentist cannaworldnews; do (cd "$s" && npm ci >/dev/null 2>&1 && BASE_PATH=/raj/$s npm run build >/dev/null 2>&1 && test -f build/index.html && grep -q "$s" build/index.html && test -d build/_app && echo "$s build OK") || { echo "$s FAILED"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - For each of vfamigos, lipool, lidentist, cannaworldnews: `<site>/package.json`, `<site>/package-lock.json`, `<site>/vite.config.ts`, `<site>/tsconfig.json`, `<site>/.npmrc`, `<site>/.gitignore`, `<site>/static/favicon.svg`, `<site>/src/app.html`, `<site>/src/app.d.ts`, `<site>/src/routes/+layout.ts`, `<site>/src/routes/+layout.svelte`, `<site>/src/routes/+page.svelte` all exist.
    - `grep -rL "ssr = false" */src/routes/+layout.ts` lists all four +layout.ts (i.e. NONE contain `ssr = false`); and `grep -l "prerender = true" */src/routes/+layout.ts` lists all four.
    - `grep -l "trailingSlash = 'always'" */src/routes/+layout.ts` lists all four.
    - `grep -l "process.env.BASE_PATH" */vite.config.ts` lists all four.
    - `grep -L "adapter-auto" */package.json` lists all four (adapter-auto absent everywhere).
    - Each `<site>/package.json` `name` matches its folder: `grep -q '"name": "vfamigos"' vfamigos/package.json` (and same for the other three).
    - `grep -q '<h1>vfamigos</h1>' vfamigos/src/routes/+page.svelte` (and lipool/lidentist/cannaworldnews respectively).
    - `grep -q 'href="../"' vfamigos/src/routes/+page.svelte` succeeds (relative back-to-hub link).
    - `grep -q "%sveltekit.assets%/favicon.svg" vfamigos/src/app.html` succeeds AND `grep -q "fonts.googleapis" vfamigos/src/app.html` FAILS (font links stripped).
    - Four committed lockfiles exist: `test -f vfamigos/package-lock.json && test -f lipool/package-lock.json && test -f lidentist/package-lock.json && test -f cannaworldnews/package-lock.json`.
    - The automated verify command prints "<site> build OK" for all four (each builds under BASE_PATH and emits build/index.html containing the site name + build/_app).
    - No nested git repo created: `test ! -d vfamigos/.git` (and the other three).
  </acceptance_criteria>
  <done>All four site folders are minimal real SvelteKit apps that build under `npm ci && BASE_PATH=/raj/<site> npm run build`, each emitting build/index.html with the site's placeholder text and a build/_app directory; every +layout.ts carries prerender=true + trailingSlash='always' and no ssr=false; adapter-auto is absent; four package-lock.json files are committed; none of the folders is a nested git repo.</done>
</task>

<task type="auto">
  <name>Task 2: Author the committed hub, 404, and .nojekyll in pages-root/</name>
  <files>pages-root/index.html, pages-root/404.html, pages-root/.nojekyll</files>
  <read_first>
    - .planning/phases/01-deploy-pipeline-walking-skeleton/01-RESEARCH.md (hub / 404 Code Examples + Open Question 1 base-href finding)
    - .planning/phases/01-deploy-pipeline-walking-skeleton/01-CONTEXT.md (D-04, D-05, D-06 and Claude's-Discretion styling: tasteful, dark-neutral, zero dependencies)
    - .github/workflows/deploy.yml (if present from plan 01-01 — the assemble job copies pages-root/index.html, pages-root/404.html, pages-root/.nojekyll into _site; these filenames must match exactly)
  </read_first>
  <action>
Create the `pages-root/` folder at the repo root with three committed files. These are plain static files (NOT built by SvelteKit); the deploy assemble job copies them into the deployment root verbatim. Styling is Claude's discretion per D-04 — the content below is tasteful, dark-neutral, zero-dependency, and MUST keep the exact link structure shown.

--- `pages-root/index.html` (the hub, always served at /raj/, so relative `./<site>/` links resolve correctly; NO <base> tag) ---
```html
<!doctype html>
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<title>raj — four sites, one deploy</title>
		<style>
			:root { color-scheme: dark; }
			* { box-sizing: border-box; }
			body {
				margin: 0; min-height: 100vh; display: grid; place-items: center;
				background: #0e0e10; color: #e6e6e6;
				font: 16px/1.5 system-ui, -apple-system, Segoe UI, Roboto, sans-serif;
				padding: 2rem;
			}
			main { width: 100%; max-width: 42rem; }
			h1 { font-size: 3rem; letter-spacing: -0.03em; margin: 0 0 0.25rem; }
			.tagline { color: #9a9a9f; margin: 0 0 2rem; }
			ul { list-style: none; margin: 0; padding: 0; display: grid; gap: 0.75rem; }
			a.card {
				display: flex; flex-direction: column; gap: 0.15rem;
				padding: 1rem 1.25rem; border: 1px solid #26262b; border-radius: 12px;
				background: #16161a; color: inherit; text-decoration: none;
				transition: border-color 0.15s ease, background 0.15s ease;
			}
			a.card:hover, a.card:focus-visible { border-color: #4c8bf5; background: #1b1b21; }
			a.card:focus-visible { outline: 2px solid #4c8bf5; outline-offset: 2px; }
			.name { font-weight: 600; font-size: 1.1rem; }
			.desc { color: #9a9a9f; font-size: 0.9rem; }
		</style>
	</head>
	<body>
		<main>
			<h1>raj</h1>
			<p class="tagline">Four sites shipping from one repo.</p>
			<ul>
				<li><a class="card" href="./vfamigos/"><span class="name">vfamigos</span><span class="desc">collectibles — coming soon</span></a></li>
				<li><a class="card" href="./lipool/"><span class="name">lipool</span><span class="desc">Long Island pools — coming soon</span></a></li>
				<li><a class="card" href="./lidentist/"><span class="name">lidentist</span><span class="desc">dentist reviews — coming soon</span></a></li>
				<li><a class="card" href="./cannaworldnews/"><span class="name">cannaworldnews</span><span class="desc">cannabis news — coming soon</span></a></li>
			</ul>
		</main>
	</body>
</html>
```

--- `pages-root/404.html` (served for ANY bad URL at the originally-requested path; `<base href="/raj/">` makes the relative links resolve from any depth — see OQ1 sign-off) ---
```html
<!doctype html>
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<base href="/raj/" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<title>Page not found — raj</title>
		<style>
			:root { color-scheme: dark; }
			* { box-sizing: border-box; }
			body {
				margin: 0; min-height: 100vh; display: grid; place-items: center;
				background: #0e0e10; color: #e6e6e6;
				font: 16px/1.5 system-ui, -apple-system, Segoe UI, Roboto, sans-serif;
				padding: 2rem; text-align: center;
			}
			main { width: 100%; max-width: 36rem; }
			h1 { font-size: 4rem; margin: 0; letter-spacing: -0.03em; }
			p { color: #9a9a9f; margin: 0.5rem 0 1.75rem; }
			nav { display: flex; flex-wrap: wrap; gap: 0.5rem; justify-content: center; }
			a {
				padding: 0.55rem 0.9rem; border: 1px solid #26262b; border-radius: 10px;
				background: #16161a; color: #e6e6e6; text-decoration: none;
			}
			a:hover, a:focus-visible { border-color: #4c8bf5; background: #1b1b21; }
			a:focus-visible { outline: 2px solid #4c8bf5; outline-offset: 2px; }
		</style>
	</head>
	<body>
		<main>
			<h1>404</h1>
			<p>That page doesn’t exist. Try one of the sites:</p>
			<nav>
				<a href="./">raj hub</a>
				<a href="vfamigos/">vfamigos</a>
				<a href="lipool/">lipool</a>
				<a href="lidentist/">lidentist</a>
				<a href="cannaworldnews/">cannaworldnews</a>
			</nav>
		</main>
	</body>
</html>
```
Note: because of `<base href="/raj/">`, `href="vfamigos/"` resolves to `/raj/vfamigos/` and `href="./"` resolves to `/raj/` regardless of how deep the bad URL was. No body link is root-absolute.

--- `pages-root/.nojekyll` (empty file — belt-and-suspenders Jekyll opt-out, D-06) ---
Create an empty file. In bash: `: > pages-root/.nojekyll`. In PowerShell: `New-Item -ItemType File -Force pages-root/.nojekyll`.
  </action>
  <verify>
    <automated>test -f pages-root/index.html && test -f pages-root/404.html && test -f pages-root/.nojekyll && grep -q "./vfamigos/" pages-root/index.html && grep -q "./lipool/" pages-root/index.html && grep -q "./lidentist/" pages-root/index.html && grep -q "./cannaworldnews/" pages-root/index.html && grep -q '<base href="/raj/">' pages-root/404.html && echo "pages-root OK"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f pages-root/index.html && test -f pages-root/404.html && test -f pages-root/.nojekyll` succeeds (all three exist).
    - `pages-root/.nojekyll` is empty: `test ! -s pages-root/.nojekyll` succeeds.
    - Hub links all four sites relatively: `grep -q './vfamigos/' pages-root/index.html && grep -q './lipool/' pages-root/index.html && grep -q './lidentist/' pages-root/index.html && grep -q './cannaworldnews/' pages-root/index.html` succeeds.
    - Hub has NO base tag and NO root-absolute site link: `grep -q '<base' pages-root/index.html` FAILS; `grep -Eq 'href="/(vfamigos|lipool|lidentist|cannaworldnews)' pages-root/index.html` FAILS.
    - 404 has the base tag: `grep -q '<base href="/raj/">' pages-root/404.html` succeeds.
    - 404 links all four sites + hub, none root-absolute in body: `grep -q 'href="vfamigos/"' pages-root/404.html && grep -q 'href="./"' pages-root/404.html` succeeds; `grep -Eq 'href="/(vfamigos|lipool|lidentist|cannaworldnews)' pages-root/404.html` FAILS.
    - 404 says not found: `grep -q '404' pages-root/404.html` succeeds.
    - Both pages carry a visible-focus style: `grep -q 'focus-visible' pages-root/index.html && grep -q 'focus-visible' pages-root/404.html` succeeds.
  </acceptance_criteria>
  <done>pages-root/ contains index.html (styled hub, four relative ./<site>/ link cards, no base tag), 404.html (styled, `<base href="/raj/">`, links four sites + hub with no root-absolute body hrefs), and an empty .nojekyll — the exact three files the deploy assemble job copies into the deployment root.</done>
</task>

</tasks>

<verification>
Local (author-time) verification — both automated commands must pass:
- Task 1: the four-site build loop prints "<site> build OK" for all four (each builds under BASE_PATH=/raj/<site> and emits build/index.html + build/_app).
- Task 2: `pages-root/{index.html,404.html,.nojekyll}` exist with correct link structure and base-tag rules (grep assertions above).

Phase-gate verification (deferred to phase completion, once plan 01-01's deploy.yml also lands and the branch is pushed):
- CI `npm ci && npm run build` succeeds per matrix leg; assemble asserts four `<site>/index.html`.
- Live smoke via `scripts/smoke-deploy.sh` (from 01-01): `/raj/` hub 200 with four site links, four subpaths 200, unknown URL 404 with a styled page linking the four sites.
</verification>

<success_criteria>
- Four SvelteKit placeholder apps exist and build under `npm ci && BASE_PATH=/raj/<site> npm run build`, emitting real HTML (site name in build/index.html) + build/_app; committed lockfiles present; no ssr=false anywhere; adapter-auto absent; no nested git repos.
- pages-root/index.html is a dependency-free styled hub linking the four sites via relative ./<site>/ links (no base tag, no root-absolute site hrefs).
- pages-root/404.html is a styled root 404 using `<base href="/raj/">` so its links resolve from any bad-URL depth, linking the four sites + hub, with no root-absolute body hrefs.
- pages-root/.nojekyll exists and is empty.
</success_criteria>

<output>
After completion, create `.planning/phases/01-deploy-pipeline-walking-skeleton/01-02-SUMMARY.md`
</output>
