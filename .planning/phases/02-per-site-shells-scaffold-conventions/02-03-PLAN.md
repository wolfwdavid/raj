---
phase: 02-per-site-shells-scaffold-conventions
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - lidentist/src/app.html
  - lidentist/src/lib/styles/tokens.css
  - lidentist/src/lib/components/SiteHeader.svelte
  - lidentist/src/lib/components/SiteFooter.svelte
  - lidentist/src/routes/+layout.svelte
  - lidentist/src/routes/+page.svelte
  - cannaworldnews/src/app.html
  - cannaworldnews/src/lib/styles/tokens.css
  - cannaworldnews/src/lib/components/SiteHeader.svelte
  - cannaworldnews/src/lib/components/SiteFooter.svelte
  - cannaworldnews/src/routes/+layout.svelte
  - cannaworldnews/src/routes/+page.svelte
autonomous: true
requirements: [QUAL-01, QUAL-03]
must_haves:
  truths:
    - "lidentist renders a calm cyan/green directory shell (Lexend+Source Sans 3) held to the highest a11y bar of the four"
    - "cannaworldnews renders a Swiss-editorial masthead shell (Libre Bodoni+Public Sans) with region nav and a footer disclaimer stub"
    - "Both sites present semantic landmarks (header/nav/main/footer), exactly one h1, a skip-link to #main, 3px focus rings, and 44px targets"
    - "Both sites pass npm run check and build clean with and without BASE_PATH, with no root-absolute href/src in emitted HTML"
  artifacts:
    - path: "lidentist/src/lib/styles/tokens.css"
      provides: "lidentist palette + font tokens"
      contains: "--color-primary: #0891B2"
    - path: "lidentist/src/lib/components/SiteHeader.svelte"
      provides: "lidentist nav (logo/Find a Dentist/How it Works/Appointment) via {base}"
      contains: "$app/paths"
    - path: "cannaworldnews/src/lib/styles/tokens.css"
      provides: "cannaworldnews palette + font tokens"
      contains: "--color-accent: #15803D"
    - path: "cannaworldnews/src/lib/components/SiteFooter.svelte"
      provides: "Swiss-editorial footer with informational-only disclaimer stub"
      contains: "Informational only"
  key_links:
    - from: "lidentist/src/routes/+layout.svelte"
      to: "lidentist/src/lib/styles/tokens.css"
      via: "import '$lib/styles/tokens.css'"
      pattern: "import '\\$lib/styles/tokens.css'"
    - from: "cannaworldnews SiteHeader region nav"
      to: "Phase-3 routes (/region/americas/ ...)"
      via: "{base}/region/<r>/ with rel=external (prerender-safe)"
      pattern: "rel=\"external\""
---

<objective>
Turn the lidentist and cannaworldnews placeholder apps into two visually distinct branded shells (nav/masthead + footer + real hero), each in its committed design system, following 02-CONVENTIONS.md exactly. lidentist is the accessibility exemplar of the four.

Purpose: Prove the remaining two design systems visually; runs parallel to 02-02 (disjoint folders).
Output: Branded shells for lidentist (calm clinical directory / a11y exemplar) and cannaworldnews (Swiss editorial publication).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-per-site-shells-scaffold-conventions/02-CONTEXT.md
@.planning/phases/02-per-site-shells-scaffold-conventions/02-CONVENTIONS.md
@.planning/design/lidentist.md
@.planning/design/cannaworldnews.md

<interfaces>
<!-- Existing per-site scaffold (identical shape in both folders). Do NOT change +layout.ts or vite.config.ts. -->
- src/routes/+layout.ts: `export const prerender = true; export const trailingSlash = 'always';` (keep; never add ssr=false)
- src/routes/+layout.svelte: currently `let { children } = $props(); {@render children()}` — replace with the 02-CONVENTIONS skeleton
- src/routes/+page.svelte: placeholder `<h1>site</h1>` — replace with the site hero (keep exactly ONE <h1>)
- src/app.html: generic <head>; add the site's Google Fonts <link> block
- Links: `import { base } from '$app/paths'` — the ONLY correct way to build internal links
- Prerender-safe nav: home `{base}/` crawlable; not-yet-existing Phase-3 routes carry rel="external"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: lidentist shell — calm clinical directory / a11y exemplar (Lexend+Source Sans 3)</name>
  <files>lidentist/src/app.html, lidentist/src/lib/styles/tokens.css, lidentist/src/lib/components/SiteHeader.svelte, lidentist/src/lib/components/SiteFooter.svelte, lidentist/src/routes/+layout.svelte, lidentist/src/routes/+page.svelte</files>
  <read_first>
    - .planning/design/lidentist.md (palette, typography, layout, a11y — this site is the a11y exemplar; copy hex/fonts verbatim)
    - .planning/phases/02-per-site-shells-scaffold-conventions/02-CONVENTIONS.md (skeletons + nav rule + a11y checklist)
    - lidentist/src/routes/+layout.svelte (placeholder to replace)
    - lidentist/src/routes/+page.svelte (placeholder to replace)
    - lidentist/src/app.html (add fonts here)
  </read_first>
  <action>
    Implement the lidentist shell per 02-CONVENTIONS.md, held to the STRICTEST a11y bar. EXACT values from design/lidentist.md:

    **lidentist/src/lib/styles/tokens.css** — `:root` verbatim:
    `--color-primary: #0891B2; --color-on-primary: #FFFFFF; --color-secondary: #22D3EE; --color-accent: #059669; --color-background: #ECFEFF; --color-surface: #FFFFFF; --color-foreground: #164E63; --color-muted: #E8F1F6; --color-muted-foreground: #64748B; --color-border: #A5F3FC; --color-destructive: #DC2626; --color-ring: #0891B2;`
    plus `--font-heading: 'Lexend', system-ui, sans-serif; --font-body: 'Source Sans 3', system-ui, sans-serif;`. Base font-size 16px minimum everywhere.

    **lidentist/src/app.html** — add preconnects + `<link href="https://fonts.googleapis.com/css2?family=Lexend:wght@400;500;600;700&family=Source+Sans+3:wght@400;500;600;700&display=swap" rel="stylesheet" />`

    **lidentist/src/lib/components/SiteHeader.svelte** — `<header>` + `<nav aria-label="Primary">`. Brand `<a class="brand" href="{base}/">Lidentist</a>` (Lexend). Nav (D-05): Find a Dentist → `#directory` (same-page section, NO rel); How it Works → `#how` (same-page, NO rel); Appointment → `{base}/appointment/` `rel="external"` styled as CTA in health-green `--color-accent` (#059669). High-contrast, ≥44px targets, generous focus states. Inline SVG icons only.

    **lidentist/src/lib/components/SiteFooter.svelte** — `<footer>` with brand line + nav echo AND a clearly visible sample-data note stub: "Sample data for demonstration — not real dentists or reviews." (Full FTC labeling copy is Phase 5 / DENT-05; this is the shell stub.)

    **lidentist/src/routes/+layout.svelte** — 02-CONVENTIONS skeleton. Because this is the a11y exemplar, ensure: strict landmark set, skip-link → #main, 3px focus ring on `--color-ring`, focus transitions instant, reduced-motion honored (near-nothing to reduce by design).

    **lidentist/src/routes/+page.svelte** — hero (D-06): exactly one `<h1>` "Find a great dentist on Long Island", a subhead, and a high-contrast primary CTA `<a class="cta" href="{base}/appointment/" rel="external">Request an appointment</a>`. Include stub section anchors `<section id="directory">` (h2 "Long Island Dentists") and `<section id="how">` (h2 "How it works") so the fragment nav lands on-page; real directory/filter is Phase 3 (DENT-01/02). Strict heading hierarchy h1→h2.

    Keep `+layout.ts` and `vite.config.ts` unchanged. Fully rewrite the placeholder +page.svelte (remove any `/raj/` comment).
  </action>
  <acceptance_criteria>
    - `lidentist/src/lib/styles/tokens.css` contains `--color-primary: #0891B2` and `--font-heading: 'Lexend'`
    - `lidentist/src/app.html` contains `family=Lexend` and `family=Source+Sans+3` and `display=swap`
    - `lidentist/src/routes/+layout.svelte` contains `import '$lib/styles/tokens.css'` and `class="skip-link" href="#main"` and `<main id="main"`
    - `lidentist/src/lib/components/SiteHeader.svelte` contains `$app/paths` and `rel="external"`
    - `lidentist/src/lib/components/SiteFooter.svelte` contains `Sample data`
    - `lidentist/src/routes/+page.svelte` contains exactly one `<h1`, `Find a great dentist`, `id="directory"`, and `id="how"`
    - `npm run check` in lidentist exits 0
    - Built HTML (BASE_PATH build) contains `<header`, `<nav`, `<main id="main"`, `<footer`, `family=Lexend`; grep `(href|src)="/[^/]` over build/**/*.html finds nothing
  </acceptance_criteria>
  <verify>
    <automated>cd lidentist && npm run check && MSYS_NO_PATHCONV=1 BASE_PATH=/raj/lidentist npm run build && grep -q "family=Lexend" build/index.html && grep -q '<main id="main"' build/index.html && test $(grep -c "<h1" build/index.html) -eq 1 && grep -q "Sample data" build/index.html && ! grep -rqE '(href|src)="/[^/]' build --include="*.html" && echo LIDENTIST_SHELL_OK</automated>
  </verify>
  <done>lidentist shows a Lexend/cyan calm directory shell with a green appointment CTA, sample-data footer note, strict landmarks + one h1 + skip-link; check passes; build portable with/without base.</done>
</task>

<task type="auto">
  <name>Task 2: cannaworldnews shell — Swiss editorial masthead (Libre Bodoni+Public Sans)</name>
  <files>cannaworldnews/src/app.html, cannaworldnews/src/lib/styles/tokens.css, cannaworldnews/src/lib/components/SiteHeader.svelte, cannaworldnews/src/lib/components/SiteFooter.svelte, cannaworldnews/src/routes/+layout.svelte, cannaworldnews/src/routes/+page.svelte</files>
  <read_first>
    - .planning/design/cannaworldnews.md (palette, typography, layout, a11y — copy hex/fonts verbatim)
    - .planning/phases/02-per-site-shells-scaffold-conventions/02-CONVENTIONS.md (skeletons + nav rule + a11y checklist)
    - cannaworldnews/src/routes/+layout.svelte (placeholder to replace)
    - cannaworldnews/src/routes/+page.svelte (placeholder to replace)
    - cannaworldnews/src/app.html (add fonts here)
  </read_first>
  <action>
    Implement the cannaworldnews shell per 02-CONVENTIONS.md with EXACT values from design/cannaworldnews.md:

    **cannaworldnews/src/lib/styles/tokens.css** — `:root` verbatim:
    `--color-primary: #18181B; --color-on-primary: #FFFFFF; --color-secondary: #3F3F46; --color-accent: #15803D; --color-background: #FAFAFA; --color-surface: #FFFFFF; --color-foreground: #09090B; --color-muted: #E8ECF0; --color-muted-foreground: #52525B; --color-border: #E4E4E7; --color-destructive: #DC2626; --color-ring: #18181B;`
    plus `--font-heading: 'Libre Bodoni', Georgia, serif; --font-body: 'Public Sans', system-ui, sans-serif;`. Accent green ONLY for region tags/kickers/links/section rules — never large green fields.

    **cannaworldnews/src/app.html** — add preconnects + `<link href="https://fonts.googleapis.com/css2?family=Libre+Bodoni:wght@400;500;600;700&family=Public+Sans:wght@300;400;500;600;700&display=swap" rel="stylesheet" />`

    **cannaworldnews/src/lib/components/SiteHeader.svelte** — editorial masthead: `<header>` with a Libre Bodoni wordmark `<a class="wordmark" href="{base}/">CannaWorldNews</a>`, a tagline "Cannabis news from around the world", and `<nav aria-label="Regions">` with FOUR region links, each `rel="external"` (Phase-3 routes): Americas → `{base}/region/americas/`; Europe → `{base}/region/europe/`; Africa → `{base}/region/africa/`; Asia-Pacific → `{base}/region/asia-pacific/`. Accent-green uppercase letterspaced region labels; hairline bottom rule. `aria-current="page"` wiring is Phase 3 (routes don't exist yet) — omit now. ≥44px targets, focus-visible underline + ring.

    **cannaworldnews/src/lib/components/SiteFooter.svelte** — `<footer>`: about note, a region index echo (same four `{base}/region/<r>/` `rel="external"` links), and the informational-only disclaimer stub line: "Informational only — not legal or medical advice." (Full compliance copy + sample-content disclaimer is Phase 5 / NEWS-05; this is the shell stub.)

    **cannaworldnews/src/routes/+layout.svelte** — 02-CONVENTIONS skeleton (tokens import, SiteHeader/SiteFooter, skip-link → #main, main id=main). Near-zero motion by design.

    **cannaworldnews/src/routes/+page.svelte** — front-page hero proving the Swiss editorial type system: exactly one `<h1>` lead headline in Libre Bodoni (large, ~40–56px), an accent-green region kicker above it (uppercase letterspaced), a dek in Public Sans, and a `<time datetime="2026-07-07">` date block. 12-col grid feel via CSS grid; hairline rules. No real articles yet (Phase 3 markdown pipeline) — a single typographic lead card is enough to prove the aesthetic.

    Keep `+layout.ts` and `vite.config.ts` unchanged. Fully rewrite the placeholder +page.svelte (remove any `/raj/` comment).
  </action>
  <acceptance_criteria>
    - `cannaworldnews/src/lib/styles/tokens.css` contains `--color-accent: #15803D` and `--font-heading: 'Libre Bodoni'`
    - `cannaworldnews/src/app.html` contains `family=Libre+Bodoni` and `family=Public+Sans` and `display=swap`
    - `cannaworldnews/src/routes/+layout.svelte` contains `import '$lib/styles/tokens.css'` and `class="skip-link" href="#main"` and `<main id="main"`
    - `cannaworldnews/src/lib/components/SiteHeader.svelte` contains `$app/paths`, `rel="external"`, and all four of `region/americas`, `region/europe`, `region/africa`, `region/asia-pacific`
    - `cannaworldnews/src/lib/components/SiteFooter.svelte` contains `Informational only`
    - `cannaworldnews/src/routes/+page.svelte` contains exactly one `<h1` and a `<time datetime=`
    - `npm run check` in cannaworldnews exits 0
    - Built HTML (BASE_PATH build) contains `<header`, `<nav`, `<main id="main"`, `<footer`, `family=Libre+Bodoni`; grep `(href|src)="/[^/]` over build/**/*.html finds nothing
  </acceptance_criteria>
  <verify>
    <automated>cd cannaworldnews && npm run check && MSYS_NO_PATHCONV=1 BASE_PATH=/raj/cannaworldnews npm run build && grep -q "family=Libre+Bodoni" build/index.html && grep -q '<main id="main"' build/index.html && test $(grep -c "<h1" build/index.html) -eq 1 && grep -q "Informational only" build/index.html && grep -q "region/asia-pacific" build/index.html && ! grep -rqE '(href|src)="/[^/]' build --include="*.html" && echo CANNAWORLDNEWS_SHELL_OK</automated>
  </verify>
  <done>cannaworldnews shows a Libre Bodoni Swiss-editorial masthead with four region nav links, a typographic lead hero with kicker + time, and an informational-only footer disclaimer stub; landmarks + one h1 + skip-link present; check passes; build portable with/without base.</done>
</task>

</tasks>

<verification>
- Both sites: `npm run check` clean; BASE_PATH and empty-base builds succeed; portability grep finds no root-absolute href/src.
- Built HTML for each has header/nav/main/footer landmarks, exactly one h1, skip-link to #main, and the correct font-family link (Lexend vs Libre Bodoni) — visibly two distinct brands (QUAL-01), distinct also from vfamigos/lipool.
- All cross-route nav anchors to Phase-3 routes (appointment, region/*) carry rel="external" (no prerender crawl failure).
</verification>

<success_criteria>
- lidentist and cannaworldnews each render a distinct branded shell in their committed design systems (QUAL-01).
- lidentist meets the highest a11y bar; both meet the baseline: landmarks, one h1, skip-link, 3px focus rings, 44px targets, reduced-motion honored, SVG icons not emoji (QUAL-03).
- Both pass the CI gates authored in 02-01 (svelte-check + portability grep).
</success_criteria>

<output>
After completion, create `.planning/phases/02-per-site-shells-scaffold-conventions/02-03-SUMMARY.md`.
</output>
