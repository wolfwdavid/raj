---
phase: 02-per-site-shells-scaffold-conventions
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - vfamigos/src/app.html
  - vfamigos/src/lib/styles/tokens.css
  - vfamigos/src/lib/components/SiteHeader.svelte
  - vfamigos/src/lib/components/SiteFooter.svelte
  - vfamigos/src/routes/+layout.svelte
  - vfamigos/src/routes/+page.svelte
  - lipool/src/app.html
  - lipool/src/lib/styles/tokens.css
  - lipool/src/lib/components/SiteHeader.svelte
  - lipool/src/lib/components/SiteFooter.svelte
  - lipool/src/routes/+layout.svelte
  - lipool/src/routes/+page.svelte
autonomous: true
requirements: [QUAL-01, QUAL-03]
must_haves:
  truths:
    - "vfamigos renders a playful rose-branded nav/footer shell (Fredoka+Nunito) with a real hero and 'Shop the Amigos' CTA"
    - "lipool renders a trust-blue nav/footer shell (Poppins+Open Sans) with a benefit-led hero, Get-a-Free-Quote CTA, and click-to-call"
    - "Both sites present semantic landmarks (header/nav/main/footer), exactly one h1, a skip-link to #main, 3px focus rings, and 44px targets"
    - "Both sites pass npm run check and build clean with and without BASE_PATH, with no root-absolute href/src in emitted HTML"
  artifacts:
    - path: "vfamigos/src/lib/styles/tokens.css"
      provides: "vfamigos palette + font tokens"
      contains: "--color-primary: #E11D48"
    - path: "vfamigos/src/lib/components/SiteHeader.svelte"
      provides: "vfamigos nav (logo/Shop/About/Cart badge) via {base}"
      contains: "$app/paths"
    - path: "lipool/src/lib/styles/tokens.css"
      provides: "lipool palette + font tokens"
      contains: "--color-primary: #0284C7"
    - path: "lipool/src/lib/components/SiteHeader.svelte"
      provides: "lipool nav (logo/Services/Gallery/Get Quote + click-to-call) via {base}"
      contains: "tel:"
  key_links:
    - from: "vfamigos/src/routes/+layout.svelte"
      to: "vfamigos/src/lib/styles/tokens.css"
      via: "import '$lib/styles/tokens.css'"
      pattern: "import '\\$lib/styles/tokens.css'"
    - from: "SiteHeader nav anchors"
      to: "Phase-3 routes (/products/, /gallery/, /quote/)"
      via: "{base}/route/ with rel=external (prerender-safe)"
      pattern: "rel=\"external\""
---

<objective>
Turn the vfamigos and lipool placeholder apps into two visually distinct branded shells (nav + footer + real hero), each in its committed design system, following 02-CONVENTIONS.md exactly.

Purpose: Prove two of the four design systems visually and lock the shell pattern; these run parallel to 02-03 (disjoint folders).
Output: Branded shells for vfamigos (playful collectible ecom) and lipool (LI pool trust/lead-gen).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-per-site-shells-scaffold-conventions/02-CONTEXT.md
@.planning/phases/02-per-site-shells-scaffold-conventions/02-CONVENTIONS.md
@.planning/design/vfamigos.md
@.planning/design/lipool.md

<interfaces>
<!-- Existing per-site scaffold (identical shape in both folders). Do NOT change +layout.ts or vite.config.ts. -->
- src/routes/+layout.ts: `export const prerender = true; export const trailingSlash = 'always';` (keep; never add ssr=false)
- src/routes/+layout.svelte: currently `let { children } = $props(); {@render children()}` — replace with the 02-CONVENTIONS skeleton
- src/routes/+page.svelte: placeholder `<h1>site</h1>` — replace with the site hero (keep exactly ONE <h1>)
- src/app.html: generic <head>; add the site's Google Fonts <link> block
- Links: `import { base } from '$app/paths'` — the ONLY correct way to build internal links
- Prerender-safe nav: home `{base}/` crawlable; not-yet-existing Phase-3 routes carry rel="external"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: vfamigos shell — playful rose ecom (Fredoka+Nunito)</name>
  <files>vfamigos/src/app.html, vfamigos/src/lib/styles/tokens.css, vfamigos/src/lib/components/SiteHeader.svelte, vfamigos/src/lib/components/SiteFooter.svelte, vfamigos/src/routes/+layout.svelte, vfamigos/src/routes/+page.svelte</files>
  <read_first>
    - .planning/design/vfamigos.md (palette table, typography, layout, a11y — copy hex/fonts verbatim)
    - .planning/phases/02-per-site-shells-scaffold-conventions/02-CONVENTIONS.md (skeletons + nav rule + a11y checklist)
    - vfamigos/src/routes/+layout.svelte (placeholder to replace)
    - vfamigos/src/routes/+page.svelte (placeholder to replace)
    - vfamigos/src/app.html (add fonts here)
  </read_first>
  <action>
    Implement the vfamigos shell per 02-CONVENTIONS.md with these EXACT values from design/vfamigos.md:

    **vfamigos/src/lib/styles/tokens.css** — `:root` with the full palette verbatim:
    `--color-primary: #E11D48; --color-on-primary: #FFFFFF; --color-secondary: #FB7185; --color-accent: #2563EB; --color-background: #FFF1F2; --color-surface: #FFFFFF; --color-foreground: #881337; --color-text: #3F1220; --color-muted: #F0ECF2; --color-border: #FECDD3; --color-destructive: #DC2626; --color-ring: #E11D48;`
    plus `--font-heading: 'Fredoka', system-ui, sans-serif; --font-body: 'Nunito', system-ui, sans-serif;`

    **vfamigos/src/app.html** — add in `<head>`:
    `<link rel="preconnect" href="https://fonts.googleapis.com" />`, `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />`, and `<link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@400;500;600;700&family=Nunito:wght@300;400;500;600;700&display=swap" rel="stylesheet" />`

    **vfamigos/src/lib/components/SiteHeader.svelte** — sticky `<header>` + `<nav aria-label="Primary">`. Brand `<a class="brand" href="{base}/">vfamigos</a>` (Fredoka). Nav items (D-05): Shop → `{base}/products/` `rel="external"`; About → `{base}/about/` `rel="external"`; Cart → `{base}/cart/` `rel="external"` with an inline SVG cart icon + a badge `<span class="cart-badge" aria-label="Cart, 0 items">0</span>` (static 0 now; live count is Phase 4). Rose brand, chunky rounded buttons (radius 16px), ≥44px targets. Inline Lucide-style SVG only, no emoji.

    **vfamigos/src/lib/components/SiteFooter.svelte** — `<footer>` with brand line "vfamigos — collect the Amigos" + a nav echo (Shop/About) using the same `{base}` + `rel="external"` rule. Rose-tinted surface.

    **vfamigos/src/routes/+layout.svelte** — the 02-CONVENTIONS skeleton: `import '$lib/styles/tokens.css'`, import SiteHeader/SiteFooter, skip-link → `#main`, `<main id="main">{@render children()}</main>`, global focus ring + reduced-motion block.

    **vfamigos/src/routes/+page.svelte** — real hero (D-06): exactly one `<h1>` (e.g. "Meet the Amigos"), a subhead brand-story line, and a primary CTA `<a class="cta" href="{base}/products/" rel="external">Shop the Amigos</a>` styled in engagement-blue `--color-accent` (#2563EB) on white text, big rounded pill, ≥44px. Optional one soft SVG blob accent behind the hero (never behind body text), disabled under prefers-reduced-motion. No product grid yet (Phase 3).

    Keep `+layout.ts` and `vite.config.ts` unchanged. Remove the placeholder `/raj/`-mentioning comment from the old +page.svelte (rewrite fully).
  </action>
  <acceptance_criteria>
    - `vfamigos/src/lib/styles/tokens.css` contains `--color-primary: #E11D48` and `--font-heading: 'Fredoka'`
    - `vfamigos/src/app.html` contains `family=Fredoka` and `family=Nunito` and `display=swap`
    - `vfamigos/src/routes/+layout.svelte` contains `import '$lib/styles/tokens.css'` and `class="skip-link" href="#main"` and `<main id="main"`
    - `vfamigos/src/lib/components/SiteHeader.svelte` contains `$app/paths` and `rel="external"` and `aria-label="Cart`
    - `vfamigos/src/routes/+page.svelte` contains exactly one `<h1` and `Shop the Amigos`
    - `npm run check` in vfamigos exits 0
    - Built HTML (BASE_PATH build) contains `<header`, `<nav`, `<main id="main"`, `<footer`, `family=Fredoka`; grep `(href|src)="/[^/]` over build/**/*.html finds nothing
  </acceptance_criteria>
  <verify>
    <automated>cd vfamigos && npm run check && MSYS_NO_PATHCONV=1 BASE_PATH=/raj/vfamigos npm run build && grep -q "family=Fredoka" build/index.html && grep -q '<main id="main"' build/index.html && test $(grep -c "<h1" build/index.html) -eq 1 && ! grep -rqE '(href|src)="/[^/]' build --include="*.html" && echo VFAMIGOS_SHELL_OK</automated>
  </verify>
  <done>vfamigos shows a rose/Fredoka branded nav + footer + hero with a blue "Shop the Amigos" CTA; landmarks + one h1 + skip-link present; check passes; build portable with/without base.</done>
</task>

<task type="auto">
  <name>Task 2: lipool shell — trust-blue LI pool lead-gen (Poppins+Open Sans)</name>
  <files>lipool/src/app.html, lipool/src/lib/styles/tokens.css, lipool/src/lib/components/SiteHeader.svelte, lipool/src/lib/components/SiteFooter.svelte, lipool/src/routes/+layout.svelte, lipool/src/routes/+page.svelte</files>
  <read_first>
    - .planning/design/lipool.md (palette table, typography, layout, a11y — copy hex/fonts verbatim)
    - .planning/phases/02-per-site-shells-scaffold-conventions/02-CONVENTIONS.md (skeletons + nav rule + a11y checklist)
    - lipool/src/routes/+layout.svelte (placeholder to replace)
    - lipool/src/routes/+page.svelte (placeholder to replace)
    - lipool/src/app.html (add fonts here)
  </read_first>
  <action>
    Implement the lipool shell per 02-CONVENTIONS.md with these EXACT values from design/lipool.md:

    **lipool/src/lib/styles/tokens.css** — `:root` verbatim:
    `--color-primary: #0284C7; --color-on-primary: #FFFFFF; --color-secondary: #06B6D4; --color-accent: #0369A1; --color-background: #F0F9FF; --color-surface: #FFFFFF; --color-foreground: #0F172A; --color-muted: #EFF7FB; --color-muted-foreground: #64748B; --color-border: #E0F0F8; --color-destructive: #DC2626; --color-ring: #0284C7;`
    plus `--font-heading: 'Poppins', system-ui, sans-serif; --font-body: 'Open Sans', system-ui, sans-serif;`. (Warm amber `#D97706` allowed later for star/badge accents only — not needed in the shell.)

    **lipool/src/app.html** — add in `<head>` the preconnects and `<link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;500;600;700&family=Poppins:wght@500;600;700&display=swap" rel="stylesheet" />`

    **lipool/src/lib/components/SiteHeader.svelte** — `<header>` + `<nav aria-label="Primary">`. Brand `<a class="brand" href="{base}/">Lipool</a>` (Poppins). Nav (D-05): Services → `#services` (same-page section, NO rel); Gallery → `{base}/gallery/` `rel="external"`; Get Quote → `{base}/quote/` `rel="external"` styled as the primary CTA (deep `--color-accent` #0369A1); plus a click-to-call `<a href="tel:+15165550100" aria-label="Call Lipool at (516) 555-0100">` with an inline SVG phone icon (placeholder LI number; real number is v2/KEYS). ≥44px targets.

    **lipool/src/lib/components/SiteFooter.svelte** — `<footer>` NAP-ready stub: brand line "Lipool — Long Island's Pool People", a NAP block placeholder (name, "Serving Nassau & Suffolk County", the same `tel:` link), and a nav echo. Full LocalBusiness JSON-LD + real NAP is Phase 3 (POOL-05) — leave a `<!-- JSON-LD: Phase 3 -->` marker only.

    **lipool/src/routes/+layout.svelte** — 02-CONVENTIONS skeleton (tokens import, SiteHeader/SiteFooter, skip-link → #main, main id=main, focus ring + reduced-motion).

    **lipool/src/routes/+page.svelte** — hero (D-06): full-width water-gradient band (primary→secondary), exactly one `<h1>` "Long Island's Pool People", subhead free-quote promise, a primary CTA `<a class="cta" href="{base}/quote/" rel="external">Get a Free Quote</a>` (accent #0369A1) and the click-to-call link. Include an empty `<section id="services">` anchor target stub (heading h2 "Our Services") so the `#services` nav lands on-page; cards are Phase 3. Motion minimal, honor reduced-motion.

    Keep `+layout.ts` and `vite.config.ts` unchanged. Fully rewrite the placeholder +page.svelte (remove any `/raj/` comment).
  </action>
  <acceptance_criteria>
    - `lipool/src/lib/styles/tokens.css` contains `--color-primary: #0284C7` and `--font-heading: 'Poppins'`
    - `lipool/src/app.html` contains `family=Poppins` and `family=Open+Sans` and `display=swap`
    - `lipool/src/routes/+layout.svelte` contains `import '$lib/styles/tokens.css'` and `class="skip-link" href="#main"` and `<main id="main"`
    - `lipool/src/lib/components/SiteHeader.svelte` contains `$app/paths`, `rel="external"`, and `tel:+15165550100`
    - `lipool/src/routes/+page.svelte` contains exactly one `<h1`, `Long Island's Pool People`, and `id="services"`
    - `npm run check` in lipool exits 0
    - Built HTML (BASE_PATH build) contains `<header`, `<nav`, `<main id="main"`, `<footer`, `family=Poppins`; grep `(href|src)="/[^/]` over build/**/*.html finds nothing
  </acceptance_criteria>
  <verify>
    <automated>cd lipool && npm run check && MSYS_NO_PATHCONV=1 BASE_PATH=/raj/lipool npm run build && grep -q "family=Poppins" build/index.html && grep -q '<main id="main"' build/index.html && test $(grep -c "<h1" build/index.html) -eq 1 && grep -q "tel:" build/index.html && ! grep -rqE '(href|src)="/[^/]' build --include="*.html" && echo LIPOOL_SHELL_OK</automated>
  </verify>
  <done>lipool shows a Poppins/water-blue branded nav + footer + hero with a "Get a Free Quote" CTA and a click-to-call link; landmarks + one h1 + skip-link present; check passes; build portable with/without base.</done>
</task>

</tasks>

<verification>
- Both sites: `npm run check` clean; BASE_PATH and empty-base builds succeed; portability grep finds no root-absolute href/src.
- Built HTML for each has header/nav/main/footer landmarks, exactly one h1, skip-link to #main, and the correct font-family link (Fredoka vs Poppins) — visibly two distinct brands (QUAL-01).
- All cross-route nav anchors to Phase-3 routes carry rel="external" (no prerender crawl failure).
</verification>

<success_criteria>
- vfamigos and lipool each render a distinct branded shell in their committed design systems (QUAL-01).
- Both meet the a11y baseline: landmarks, one h1, skip-link, 3px focus rings, 44px targets, reduced-motion honored, SVG icons not emoji (QUAL-03).
- Both pass the CI gates authored in 02-01 (svelte-check + portability grep).
</success_criteria>

<output>
After completion, create `.planning/phases/02-per-site-shells-scaffold-conventions/02-02-SUMMARY.md`.
</output>
