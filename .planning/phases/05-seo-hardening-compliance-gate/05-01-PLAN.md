---
phase: 05-seo-hardening-compliance-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vfamigos/src/lib/components/Seo.svelte
  - lipool/src/lib/components/Seo.svelte
  - lidentist/src/lib/components/Seo.svelte
  - cannaworldnews/src/lib/components/Seo.svelte
  - vfamigos/src/lib/config.ts
  - lipool/src/lib/config.ts
  - lidentist/src/lib/config.ts
  - cannaworldnews/src/lib/config.ts
  - vfamigos/src/routes/+page.svelte
  - vfamigos/src/routes/products/+page.svelte
  - vfamigos/src/routes/products/[slug]/+page.svelte
  - vfamigos/src/routes/about/+page.svelte
  - vfamigos/src/routes/cart/+page.svelte
  - vfamigos/src/routes/thanks/+page.svelte
  - lipool/src/routes/+page.svelte
  - lipool/src/routes/gallery/+page.svelte
  - lipool/src/routes/quote/+page.svelte
  - lidentist/src/routes/+page.svelte
  - lidentist/src/routes/dentists/[slug]/+page.svelte
  - lidentist/src/routes/appointment/+page.svelte
  - cannaworldnews/src/routes/+page.svelte
  - cannaworldnews/src/routes/region/[region]/+page.svelte
  - cannaworldnews/src/routes/articles/[slug]/+page.svelte
autonomous: true
requirements: [QUAL-04]

must_haves:
  truths:
    - "Every page across all four sites emits a unique, non-empty <title> and meta description in its built HTML"
    - "Every page's canonical and og:url are absolute URLs derived from SITE_URL (config knob), never from BASE_PATH"
    - "Setting VITE_SITE_URL changes the canonical/OG origin; changing BASE_PATH does not"
    - "The existing portability guard still passes — canonical/OG are https:// absolute URLs in <head>, not root-absolute nav href/src"
  artifacts:
    - path: "vfamigos/src/lib/components/Seo.svelte"
      provides: "Hand-rolled per-page <svelte:head> meta emitter (title/description/canonical/OG/twitter)"
      contains: 'rel="canonical"'
    - path: "cannaworldnews/src/lib/config.ts"
      provides: "SITE_URL config knob for cannaworldnews (new file — no config.ts existed)"
      contains: "SITE_URL"
    - path: "vfamigos/src/routes/products/[slug]/+page.svelte"
      provides: "Dynamic PDP with per-record <Seo> (title/description from product)"
      contains: "<Seo"
  key_links:
    - from: "Seo.svelte (each site)"
      to: "$lib/config SITE_URL"
      via: "import { SITE_URL } from '$lib/config'"
      pattern: "import.*SITE_URL.*from '\\$lib/config'"
    - from: "every +page.svelte"
      to: "Seo.svelte"
      via: "<Seo title=... description=... path=... />"
      pattern: "<Seo"
    - from: "Seo.svelte"
      to: "canonical/og:url"
      via: "SITE_URL + path (absolute, not BASE_PATH)"
      pattern: 'rel="canonical" href='
---

<objective>
Give every page across all four sites (vfamigos, lipool, lidentist, cannaworldnews) a unique title + meta description + absolute canonical + Open Graph/Twitter tags, all derived from a per-site `SITE_URL` config knob (NOT `BASE_PATH`). This makes every site custom-domain-ready: one config value flips from the github.io subpath to the real domain at cutover, and all indexable URLs follow.

Purpose: QUAL-04 — per-page titles + meta descriptions; canonical/OG URLs derive from a configurable SITE_URL (never BASE_PATH). This is the SEO-hardening half of the final v1 phase.
Output: A duplicated-per-site `Seo.svelte` component, a `SITE_URL` export in each site's `config.ts` (created new for cannaworldnews), and a `<Seo>` invocation on all 15 pages/routes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-seo-hardening-compliance-gate/05-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Contracts the executor needs. Use these directly — no codebase exploration required. -->

Existing config.ts pattern (VITE_ override + '' fallback), from vfamigos/src/lib/config.ts:
```ts
export const STRIPE_PAYMENT_LINK: string = import.meta.env.VITE_STRIPE_PAYMENT_LINK ?? '';
```
lipool/lidentist expose `FORM_ENDPOINT` the same way. cannaworldnews has NO config.ts yet.

Vite base config (base comes from BASE_PATH env — SITE_URL is SEPARATE and must NOT use it), from vfamigos/vite.config.ts:
```ts
paths: { base: (process.env.BASE_PATH ?? '') as '' | `/${string}` }
```

Article record shape (cannaworldnews), from cannaworldnews/src/lib/articles.ts:
```ts
export interface Article { slug; title; dek; region; date; author; html }  // date is 'YYYY-MM-DD' (valid ISO)
```
Region loader returns `{ region, label, articles }` (cannaworldnews/src/routes/region/[region]/+page.ts).

Product record shape (vfamigos), from vfamigos/src/lib/data/products.ts:
```ts
export interface Product { slug; name; series; price; tagline; bio; traits; accent }
```

Dentist record shape (lidentist), from lidentist/src/lib/data/dentists.ts:
```ts
export interface Dentist { slug; name; practice; town; specialty; rating; reviewCount; reviews }
```

Existing portability guard (deploy.yml) — this is what canonical/OG must NOT trip:
```
grep -rnE '(href|src)="/[^/]' build --include="*.html"
```
It matches only href/src whose value starts with a single `/`. Our canonical is `href="https://..."` and og:url is a `content="https://..."` attribute → neither matches. SAFE. Do NOT emit any root-absolute href/src.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seo.svelte component + SITE_URL config knob (per site)</name>
  <files>vfamigos/src/lib/components/Seo.svelte, lipool/src/lib/components/Seo.svelte, lidentist/src/lib/components/Seo.svelte, cannaworldnews/src/lib/components/Seo.svelte, vfamigos/src/lib/config.ts, lipool/src/lib/config.ts, lidentist/src/lib/config.ts, cannaworldnews/src/lib/config.ts</files>
  <read_first>
    - vfamigos/src/lib/config.ts (existing VITE_ override pattern to extend, per D-01)
    - lipool/src/lib/config.ts and lidentist/src/lib/config.ts (same pattern; append SITE_URL)
    - cannaworldnews/src/lib/articles.ts (confirm cannaworldnews has NO config.ts — you CREATE it)
    - .planning/phases/05-seo-hardening-compliance-gate/05-CONTEXT.md (D-01, D-02, D-07)
    - .planning/research/STACK.md ("SEO pattern" section — hand-rolled <Seo> via <svelte:head>, SITE_URL absolute, no library)
  </read_first>
  <action>
    This is the interface-first foundation: create the component every page imports and the config value it reads.

    (A) Add `SITE_URL` to each site's `config.ts` per D-01. SITE_URL is the site's indexable canonical origin, defaulting to the LIVE GitHub Pages origin+subpath (so canonical/OG resolve NOW) and flippable to the future custom domain via `VITE_SITE_URL` — a single knob, no code change. CRITICAL: SITE_URL is unrelated to BASE_PATH. NO trailing slash on SITE_URL (paths passed to <Seo> carry the leading+trailing slash).

    vfamigos/src/lib/config.ts — APPEND (keep existing STRIPE_PAYMENT_LINK):
    ```ts
    // SITE_URL: the site's indexable canonical origin. Drives absolute canonical + OG URLs
    // (NEVER BASE_PATH). Defaults to the live Pages origin+subpath so URLs resolve today; at
    // custom-domain cutover set VITE_SITE_URL='https://vfamigos.com' — one knob, no code change.
    export const SITE_URL: string = import.meta.env.VITE_SITE_URL ?? 'https://wolfwdavid.github.io/raj/vfamigos';
    ```
    lipool/src/lib/config.ts — APPEND (keep FORM_ENDPOINT); default `'https://wolfwdavid.github.io/raj/lipool'`, cutover comment `'https://lipool.com'`.
    lidentist/src/lib/config.ts — APPEND (keep FORM_ENDPOINT); default `'https://wolfwdavid.github.io/raj/lidentist'`, cutover comment `'https://lidentist.reviews'`.
    cannaworldnews/src/lib/config.ts — CREATE NEW FILE (no config existed); default `'https://wolfwdavid.github.io/raj/cannaworldnews'`, cutover comment `'https://cannaworldnews.com'`.

    (B) Create `Seo.svelte` in EACH site's `src/lib/components/` — identical except the `SITE_NAME` constant. Per D-02 it uses `<svelte:head>`, takes `{ title, description, path, type?, publishedTime? }`, and emits title/description/canonical/OG/Twitter with absolute SITE_URL-derived URLs. Full component (vfamigos shown; change SITE_NAME per site):
    ```svelte
    <script lang="ts">
      import { SITE_URL } from '$lib/config';

      interface Props {
        title: string;
        description: string;
        path: string; // logical route path, leading + trailing slash, e.g. '/products/pablo-the-brave/'
        type?: string; // 'website' (default) | 'article'
        publishedTime?: string; // ISO date; only for type='article'
      }
      let { title, description, path, type = 'website', publishedTime }: Props = $props();

      const SITE_NAME = 'Vfamigos'; // Lipool | Lidentist | CannaWorldNews per site
      // Absolute, SITE_URL-derived — NOT BASE_PATH. This is the whole point of QUAL-04.
      const url = SITE_URL + path;
    </script>

    <svelte:head>
      <title>{title}</title>
      <meta name="description" content={description} />
      <link rel="canonical" href={url} />

      <meta property="og:type" content={type} />
      <meta property="og:site_name" content={SITE_NAME} />
      <meta property="og:title" content={title} />
      <meta property="og:description" content={description} />
      <meta property="og:url" content={url} />
      {#if publishedTime}
        <meta property="article:published_time" content={publishedTime} />
      {/if}

      <meta name="twitter:card" content="summary" />
      <meta name="twitter:title" content={title} />
      <meta name="twitter:description" content={description} />
    </svelte:head>
    ```
    SITE_NAME per site: vfamigos → `'Vfamigos'`, lipool → `'Lipool'`, lidentist → `'Lidentist'`, cannaworldnews → `'CannaWorldNews'`. No og:image in v1 (no binary assets — D per 05-CONTEXT specifics). The `type`/`publishedTime` props are wired now but only USED by cannaworldnews articles in plan 05-02; leaving them unused here is intentional (interface-first).
  </action>
  <acceptance_criteria>
    - vfamigos/src/lib/config.ts contains `SITE_URL` and `VITE_SITE_URL`; same for lipool + lidentist (each still contains its prior STRIPE_PAYMENT_LINK / FORM_ENDPOINT export).
    - cannaworldnews/src/lib/config.ts EXISTS and contains `SITE_URL` + `VITE_SITE_URL`.
    - Each of the 4 `Seo.svelte` contains `<svelte:head>`, `rel="canonical"`, `property="og:url"`, and `import { SITE_URL } from '$lib/config'`.
    - `SITE_NAME` differs per site (grep each Seo.svelte: vfamigos→Vfamigos, lipool→Lipool, lidentist→Lidentist, cannaworldnews→CannaWorldNews).
    - No SITE_URL value ends in a trailing slash.
  </acceptance_criteria>
  <verify>
    <automated>cd vfamigos && npm run check && cd ../cannaworldnews && npm run check && cd .. && grep -rl "SITE_URL" vfamigos/src/lib/config.ts lipool/src/lib/config.ts lidentist/src/lib/config.ts cannaworldnews/src/lib/config.ts && grep -rl 'rel="canonical"' vfamigos/src/lib/components/Seo.svelte lipool/src/lib/components/Seo.svelte lidentist/src/lib/components/Seo.svelte cannaworldnews/src/lib/components/Seo.svelte</automated>
  </verify>
  <done>All 4 Seo.svelte and 4 config.ts exist with the exact contracts above; svelte-check passes on the two representative sites (vfamigos, cannaworldnews); components compile (unused is fine at this stage).</done>
</task>

<task type="auto">
  <name>Task 2: Apply <Seo> to every vfamigos + lipool page</name>
  <files>vfamigos/src/routes/+page.svelte, vfamigos/src/routes/products/+page.svelte, vfamigos/src/routes/products/[slug]/+page.svelte, vfamigos/src/routes/about/+page.svelte, vfamigos/src/routes/cart/+page.svelte, vfamigos/src/routes/thanks/+page.svelte, lipool/src/routes/+page.svelte, lipool/src/routes/gallery/+page.svelte, lipool/src/routes/quote/+page.svelte</files>
  <read_first>
    - vfamigos/src/routes/+page.svelte and vfamigos/src/routes/products/[slug]/+page.svelte (current script/import pattern; dynamic page derives `product` from `data`)
    - lipool/src/routes/quote/+page.svelte (ALREADY has a <svelte:head> block with title+description — you REPLACE it with <Seo>)
    - vfamigos/src/lib/components/Seo.svelte (the component contract from Task 1)
    - .planning/phases/05-seo-hardening-compliance-gate/05-CONTEXT.md (D-03 — unique title/description per page; dynamic pages compute from the record)
  </read_first>
  <action>
    In each page: add `import Seo from '$lib/components/Seo.svelte';` to the `<script>` and place `<Seo ... />` as the FIRST element of the markup (before existing content). Titles follow the `<page> — Vfamigos` / `<page> — Lipool` pattern (D-03). Descriptions are unique and non-empty. Exact invocations (microcopy is Claude's discretion but must stay unique + non-empty — these are recommended values):

    vfamigos:
    - `/` (routes/+page.svelte): `<Seo title="Vfamigos — Original Collectible Amigo Characters" description="Meet the Amigos: a cast of original collectible characters, each with their own story, series, and superpower. Start your collection." path="/" />`
    - `/products/` (products/+page.svelte): `<Seo title="Shop All Amigos — Vfamigos" description="Browse the full collection of original Amigo collectible characters — pick the personality that speaks to you." path="/products/" />`
    - `/products/[slug]/` (products/[slug]/+page.svelte): dynamic — `product` is already `$derived(data.product)`. Add:
      `<Seo title={`${product.name} — Vfamigos`} description={product.tagline} path={`/products/${product.slug}/`} />`
    - `/about/`: `<Seo title="Our Story — Vfamigos" description="Why the Amigos? Every character is an original, built to carry a little courage, kindness, or curiosity — no borrowed faces." path="/about/" />`
    - `/cart/`: `<Seo title="Your Cart — Vfamigos" description="Review your Amigo collection and check out securely with Stripe." path="/cart/" />`
    - `/thanks/`: `<Seo title="Thank You — Vfamigos" description="Your order is confirmed. A receipt and confirmation follow by email from Stripe." path="/thanks/" />`

    lipool:
    - `/` (routes/+page.svelte): `<Seo title="Lipool — Long Island Pool Installation, Service & Renovation" description="Free, no-obligation pool quotes across Nassau & Suffolk County. Licensed, insured, and local." path="/" />`
    - `/gallery/`: `<Seo title="Project Gallery — Lipool" description="See recent Long Island pool installation, service, and renovation projects across Nassau & Suffolk." path="/gallery/" />`
    - `/quote/` (quote/+page.svelte): REPLACE the existing `<svelte:head>...</svelte:head>` block entirely with:
      `<Seo title="Get a Free Pool Quote — Lipool" description="Request a free, no-obligation pool quote for installation, service, or renovation across Nassau & Suffolk County." path="/quote/" />`
      (Remove the old `<svelte:head>` so there is no duplicate `<title>`.)

    Do NOT introduce any root-absolute href/src. Do NOT touch `{base}`-relative nav links.
  </action>
  <acceptance_criteria>
    - All 6 vfamigos pages + all 3 lipool pages contain `<Seo` and `import Seo from '$lib/components/Seo.svelte'`.
    - lipool/src/routes/quote/+page.svelte no longer contains a raw `<svelte:head>` block (replaced by <Seo>); exactly one title source.
    - Built vfamigos home `build/index.html` contains a canonical `<link ... rel="canonical" ...>` whose value starts `https://wolfwdavid.github.io/raj/vfamigos/`.
    - Built vfamigos PDP `build/products/*/index.html` each contain a `<title>` with the product name and a `rel="canonical" href="https://` line.
    - Portability guard still passes: no `(href|src)="/[^/]` in either site's built HTML.
  </acceptance_criteria>
  <verify>
    <automated>cd vfamigos && npm run check && BASE_PATH=/raj/vfamigos npm run build && grep -E '<link[^>]*rel="canonical"[^>]*>' build/index.html | grep -q 'https://wolfwdavid.github.io/raj/vfamigos/' && grep -rq '<title>' build/products/*/index.html && ! grep -rnE '(href|src)="/[^/]' build --include="*.html" && cd ../lipool && npm run check && BASE_PATH=/raj/lipool npm run build && grep -E '<link[^>]*rel="canonical"[^>]*>' build/quote/index.html | grep -q 'https://wolfwdavid.github.io/raj/lipool/quote/' && ! grep -rnE '(href|src)="/[^/]' build --include="*.html"</automated>
  </verify>
  <done>vfamigos (6 pages) and lipool (3 pages) each carry a unique <Seo>; both build under BASE_PATH; canonical/OG are SITE_URL-derived absolute URLs; portability guard clean; svelte-check passes.</done>
</task>

<task type="auto">
  <name>Task 3: Apply <Seo> to every lidentist + cannaworldnews page</name>
  <files>lidentist/src/routes/+page.svelte, lidentist/src/routes/dentists/[slug]/+page.svelte, lidentist/src/routes/appointment/+page.svelte, cannaworldnews/src/routes/+page.svelte, cannaworldnews/src/routes/region/[region]/+page.svelte, cannaworldnews/src/routes/articles/[slug]/+page.svelte</files>
  <read_first>
    - lidentist/src/routes/appointment/+page.svelte (ALREADY has a <svelte:head> — REPLACE with <Seo>)
    - lidentist/src/routes/dentists/[slug]/+page.svelte (dynamic; `dentist` = `$derived(data.dentist)`)
    - cannaworldnews/src/routes/articles/[slug]/+page.svelte (dynamic; `article` = `$derived(data.article)`, has `article.date` ISO + `article.dek`)
    - cannaworldnews/src/routes/region/[region]/+page.svelte (`data.label`, `data.region`, `data.articles`)
    - lidentist/src/lib/components/Seo.svelte and cannaworldnews/src/lib/components/Seo.svelte (Task 1 contract)
    - .planning/phases/05-seo-hardening-compliance-gate/05-CONTEXT.md (D-03; NOTE: article type='article'+publishedTime is plan 05-02, NOT here)
  </read_first>
  <action>
    Same pattern: import Seo, place `<Seo />` first in markup. For dynamic pages compute title/description from the record. Guard descriptions to be non-empty (fall back to title if a field could be empty).

    lidentist:
    - `/` (routes/+page.svelte, directory): `<Seo title="Lidentist — Find a Long Island Dentist" description="Browse and filter Long Island dentists by specialty and town. Sample data for demonstration." path="/" />`
    - `/dentists/[slug]/`: `<Seo title={`${dentist.name}, ${dentist.practice} — Lidentist`} description={`${dentist.name} — ${dentist.specialty} dentist in ${dentist.town}, Long Island. Sample data for demonstration.`} path={`/dentists/${dentist.slug}/`} />`
    - `/appointment/`: REPLACE the existing `<svelte:head>` block with:
      `<Seo title="Request an Appointment — Lidentist" description="Request a dental appointment on Long Island. Choose a dentist or leave it open — the office will reach out to confirm." path="/appointment/" />`

    cannaworldnews (article page gets a BASIC website-type <Seo> here — plan 05-02 upgrades it to type='article' + publishedTime for NEWS-04; do NOT add those props now):
    - `/` (routes/+page.svelte, front): `<Seo title="CannaWorldNews — Global Cannabis Policy & Industry News" description="Cannabis policy, science, and industry developments from every region. Informational only — not legal or medical advice." path="/" />`
    - `/region/[region]/`: `<Seo title={`${data.label} Cannabis News — CannaWorldNews`} description={`Cannabis policy, regulation, and industry developments across ${data.label}, reported and source-attributed.`} path={`/region/${data.region}/`} />`
    - `/articles/[slug]/`: `<Seo title={`${article.title} — CannaWorldNews`} description={article.dek || article.title} path={`/articles/${article.slug}/`} />`
      (`article.dek || article.title` guarantees a non-empty description even if a dek is blank.)

    Do NOT introduce root-absolute href/src. Do NOT alter the existing `{base}`-relative links, the `<time datetime>` byline, or the SampleDataBanner.
  </action>
  <acceptance_criteria>
    - All 3 lidentist pages + all 3 cannaworldnews pages contain `<Seo` and the Seo import.
    - lidentist/src/routes/appointment/+page.svelte no longer contains a raw `<svelte:head>` block.
    - Built lidentist dentist detail `build/dentists/*/index.html` each contain a `<title>` with the dentist name + `rel="canonical" href="https://` (SITE_URL-derived).
    - Built cannaworldnews article `build/articles/*/index.html` each contain a `<title>` ending "— CannaWorldNews" and a non-empty meta description; the article page's og:type is still `website` here (article upgrade happens in 05-02).
    - Both sites' built HTML pass the portability guard.
  </acceptance_criteria>
  <verify>
    <automated>cd lidentist && npm run check && BASE_PATH=/raj/lidentist npm run build && grep -rq '<title>' build/dentists/*/index.html && grep -rE '<link[^>]*rel="canonical"[^>]*>' build/dentists/*/index.html | grep -q 'https://wolfwdavid.github.io/raj/lidentist/dentists/' && ! grep -rnE '(href|src)="/[^/]' build --include="*.html" && cd ../cannaworldnews && npm run check && BASE_PATH=/raj/cannaworldnews npm run build && grep -rq 'CannaWorldNews</title>' build/articles/*/index.html && grep -rE 'name="description"[^>]*content="[^"]+"' build/articles/*/index.html >/dev/null && ! grep -rnE '(href|src)="/[^/]' build --include="*.html"</automated>
  </verify>
  <done>lidentist (3 pages) and cannaworldnews (3 pages) each carry a unique <Seo>; both build under BASE_PATH with SITE_URL-derived canonical/OG; portability guard clean; svelte-check passes. All 15 pages across the four sites now emit unique title + description + canonical (QUAL-04 met).</done>
</task>

</tasks>

<verification>
- All four sites: `npm run check` clean, `BASE_PATH=/raj/<site> npm run build` succeeds.
- Every built `index.html` across all four sites has a non-empty `<title>`, a `meta name="description"`, and a `<link rel="canonical" href="https://...">` whose origin is the SITE_URL default (`https://wolfwdavid.github.io/raj/<site>`), never a `/raj/`-relative or root-absolute value.
- Custom-domain readiness spot-check: `VITE_SITE_URL=https://vfamigos.com BASE_PATH=/raj/vfamigos npm run build` in vfamigos yields canonical `https://vfamigos.com/...` (origin from SITE_URL) while asset/nav paths still respect BASE_PATH — proving canonical derives from SITE_URL, not BASE_PATH.
- Portability guard passes for all four sites (canonical/OG https:// absolutes do not trip `(href|src)="/[^/]`).
</verification>

<success_criteria>
- 4× Seo.svelte + 4× config.ts (SITE_URL) exist; cannaworldnews config.ts created.
- All 15 pages/routes invoke `<Seo>` with a unique, non-empty title + description.
- canonical + og:url are absolute and SITE_URL-derived on every page.
- No new portability-guard failures; all four sites still build with and without BASE_PATH.
</success_criteria>

<output>
After completion, create `.planning/phases/05-seo-hardening-compliance-gate/05-01-SUMMARY.md`
</output>
