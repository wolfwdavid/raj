---
phase: 03-content-dynamic-routes
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lipool/src/lib/data/services.ts
  - lipool/src/lib/data/gallery.ts
  - lipool/src/routes/+page.svelte
  - lipool/src/routes/gallery/+page.svelte
  - lipool/src/lib/components/SiteFooter.svelte
autonomous: true
requirements: [POOL-01, POOL-02, POOL-04, POOL-05]

must_haves:
  truths:
    - "A homeowner sees a benefit-led home with services and Long Island trust signals (licensed/insured, years, Nassau & Suffolk, free estimates)"
    - "A homeowner views a project gallery with descriptive alt text on every tile"
    - "A homeowner can tap a click-to-call tel: link present on every page"
    - "The footer emits HomeAndConstructionBusiness JSON-LD with areaServed + NAP and NO aggregateRating"
  artifacts:
    - path: "lipool/src/lib/data/services.ts"
      provides: "Typed service cards (installation, liner, openings/closings, maintenance, repairs, renovation)"
      contains: "export const services"
    - path: "lipool/src/routes/gallery/+page.svelte"
      provides: "Project gallery grid with descriptive alt text"
    - path: "lipool/src/lib/components/SiteFooter.svelte"
      provides: "HomeAndConstructionBusiness JSON-LD + visible NAP (no aggregateRating)"
      contains: "HomeAndConstructionBusiness"
  key_links:
    - from: "lipool/src/routes/+page.svelte"
      to: "lipool/src/lib/data/services.ts"
      via: "import { services }"
      pattern: "from '\\$lib/data/services'"
    - from: "lipool/src/lib/components/SiteFooter.svelte"
      to: "structured data"
      via: "application/ld+json script"
      pattern: "application/ld\\+json"
---

<objective>
Give lipool its real content: a benefit-led home with a Long Island trust-signals strip + services section, a project gallery page with descriptive alt text, a verified click-to-call `tel:` on every page, and a footer emitting `HomeAndConstructionBusiness` JSON-LD with `areaServed` + consistent NAP — deliberately WITHOUT `aggregateRating` (no visible on-page reviews → Google spammy-structured-data penalty).

Purpose: Satisfies POOL-01 (services + trust signals), POOL-02 (gallery), POOL-04 (click-to-call), POOL-05 (JSON-LD/NAP, no faked rating).
Output: services.ts, gallery.ts, home trust strip + services, /gallery/ route, footer JSON-LD + NAP.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-content-dynamic-routes/03-CONTEXT.md
@.planning/design/lipool.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@lipool/src/routes/+page.svelte
@lipool/src/lib/components/SiteHeader.svelte
@lipool/src/lib/components/SiteFooter.svelte

<interfaces>
Prerender contract (already in lipool/src/routes/+layout.ts — DO NOT change):
```ts
export const prerender = true;
export const trailingSlash = 'always';
```

Data models (Task 1 defines):
```ts
// lipool/src/lib/data/services.ts
export interface Service { slug: string; title: string; blurb: string; }
export const services: Service[]; // 6: installation, liner replacement, openings/closings, maintenance, repairs, renovation

// lipool/src/lib/data/gallery.ts
export interface GalleryItem { id: string; caption: string; alt: string; accent: string; }
export const gallery: GalleryItem[]; // 6-9 CSS-tile "projects" with DESCRIPTIVE alt text
```

Existing footer facts to reuse for NAP consistency (from lipool/src/lib/components/SiteFooter.svelte):
- Phone: tel:+15165550100  → "(516) 555-0100"
- Brand: "Lipool — Long Island's Pool People"
- Area: "Nassau & Suffolk County"
(The tel: click-to-call already exists in SiteHeader + SiteFooter — POOL-04. Verify present; do not remove.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: services.ts + gallery.ts data; home trust strip + services section</name>
  <read_first>
    - lipool/src/routes/+page.svelte (existing hero with #services anchor placeholder — extend)
    - .planning/design/lipool.md §"Layout Patterns" (trust strip: Licensed & Insured · 20+ Years · Nassau & Suffolk · Free Estimates with SVG icons; services 6 cards icon+blurb+"Get quote" link)
    - The data models in this plan's &lt;interfaces&gt;
    - .planning/phases/03-content-dynamic-routes/03-CONTEXT.md D-10
  </read_first>
  <action>
    Create `lipool/src/lib/data/services.ts` (6 `Service` records: installation, liner replacement, openings/closings, maintenance, repairs, renovation — each `slug`, `title`, 1–2 sentence `blurb`) and `lipool/src/lib/data/gallery.ts` (6–9 `GalleryItem` with descriptive `alt` text like "In-ground gunite pool with bluestone coping in Huntington", a short `caption`, and an `accent` hex for the CSS placeholder tile — no binary images, D-01/D-10).

    Edit `lipool/src/routes/+page.svelte`: keep the benefit hero. ADD (a) a trust-signals strip — 4 items (Licensed & Insured, 20+ Years, Nassau & Suffolk, Free Estimates) each an inline SVG icon (Lucide-style, never emoji) + label; and (b) the `#services` section mapping `services` to cards (icon + title + blurb + a "Get a quote" link to `{base}/quote/` with `rel="external"` — Phase-4 route, KEEP the guard). Also add a gallery-preview teaser linking to `{base}/gallery/` (internal, NO rel — /gallery/ is built in Task 2, which is where the first full build runs, so the teaser is a crawlable internal link by then).

    Build-order note: this Task's `<verify>` does NOT run `npm run build` — the first full build is Task 2, AFTER `/gallery/` exists. So the no-rel gallery teaser never gets crawled while dangling. (This is the correct pattern the other Wave-1 plans mirror.)
  </action>
  <acceptance_criteria>
    - `lipool/src/lib/data/services.ts` contains `export const services` and `grep -c 'slug:' ` returns 6
    - `lipool/src/lib/data/gallery.ts` contains `export const gallery` and every item has an `alt:`
    - `lipool/src/routes/+page.svelte` imports services and renders the 4 trust-signal labels (grep for "Licensed & Insured", "Nassau & Suffolk", "Free Estimates")
    - Quote links keep `rel="external"` (Phase-4): `grep -q 'rel="external"' lipool/src/routes/+page.svelte` passes
    - Gallery teaser has NO rel: no line containing `gallery/` also contains `rel=` (`! grep -E 'gallery/' lipool/src/routes/+page.svelte | grep -q 'rel='`)
    - `cd lipool && npm run check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd lipool && npm run check && grep -q "Licensed & Insured" src/routes/+page.svelte && grep -q "Nassau & Suffolk" src/routes/+page.svelte && grep -q 'rel="external"' src/routes/+page.svelte && ! grep -E 'gallery/' src/routes/+page.svelte | grep -q 'rel=' && echo "home OK"</automated>
  </verify>
  <done>Home shows benefit hero + LI trust strip + 6 services + gallery teaser; quote CTAs stay guarded (rel="external" asserted) and the gallery teaser stays rel-free (asserted).</done>
</task>

<task type="auto">
  <name>Task 2: /gallery/ project gallery route with descriptive alt + verify tel:</name>
  <read_first>
    - lipool/src/lib/data/gallery.ts (Task 1)
    - .planning/design/lipool.md §"Layout Patterns" (gallery: responsive grid of project tiles, descriptive alts, lazy-loaded) and §"Accessibility Notes"
    - lipool/src/lib/components/SiteHeader.svelte (confirm tel: click-to-call present — POOL-04)
  </read_first>
  <action>
    Create `lipool/src/routes/gallery/+page.svelte`: single `<h1>` (e.g. "Our Long Island Projects") + a responsive grid over `gallery` — each tile is a CSS-generated placeholder (solid/gradient `accent` field, no binary asset) with the item's descriptive `caption` visible and, since these are decorative CSS tiles standing in for photos, present the descriptive `alt` text as a visible caption / `aria-label` on each figure so the descriptive text is real on the page (POOL-02). Use `<figure>`/`<figcaption>`. Static route (auto-prerendered). Note "Representative work" labeling per Pitfall 10 (these are demonstration tiles, not real job photos).

    Verify the click-to-call `tel:+15165550100` link is present in `SiteHeader.svelte` (POOL-04 — every page). If somehow absent, add it back per the design spec; otherwise leave unchanged.
  </action>
  <acceptance_criteria>
    - `lipool/src/routes/gallery/+page.svelte` exists with exactly one `<h1>` and uses `<figure>`/`<figcaption>`
    - After `MSYS_NO_PATHCONV=1 BASE_PATH=/raj/lipool npm run build`: `lipool/build/gallery/index.html` exists and contains a gallery caption from data
    - `grep -q 'href="tel:' lipool/src/lib/components/SiteHeader.svelte` (click-to-call present)
    - `cd lipool && npm run check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd lipool && npm run check && MSYS_NO_PATHCONV=1 BASE_PATH=/raj/lipool npm run build && ls build/gallery/index.html && grep -q 'href="tel:' src/lib/components/SiteHeader.svelte && echo "gallery+tel OK"</automated>
  </verify>
  <done>/gallery/ prerenders a project grid with descriptive alt/caption on every tile; tel: click-to-call confirmed on every page.</done>
</task>

<task type="auto">
  <name>Task 3: Footer HomeAndConstructionBusiness JSON-LD + visible NAP (NO aggregateRating)</name>
  <read_first>
    - lipool/src/lib/components/SiteFooter.svelte (has the `<!-- JSON-LD: Phase 3 -->` marker + existing NAP block + tel:)
    - .planning/design/lipool.md §"Layout Patterns" (footer NAP block + LocalBusiness JSON-LD) 
    - .planning/research/FEATURES.md (the aggregateRating/visible-reviews SEO trap) + .planning/research/PITFALLS.md Pitfall 12 / spammy-structured-data
    - .planning/phases/03-content-dynamic-routes/03-CONTEXT.md D-11
  </read_first>
  <action>
    Edit `lipool/src/lib/components/SiteFooter.svelte` to emit a `HomeAndConstructionBusiness` JSON-LD script and a VISIBLE NAP block whose values match the JSON-LD 1:1.

    Build the JSON-LD object in the `<script lang="ts">` and inject it as a real `<script type="application/ld+json">` via `{@html}` (Svelte treats a literal `<script>` in markup specially, so use the `{@html}` string form):
    ```svelte
    <script lang="ts">
      const jsonLd = {
        '@context': 'https://schema.org',
        '@type': 'HomeAndConstructionBusiness',
        name: 'Lipool',
        telephone: '+1-516-555-0100',
        areaServed: ['Nassau County', 'Suffolk County'],
        address: {
          '@type': 'PostalAddress',
          streetAddress: '<placeholder street>',
          addressLocality: '<LI town>',
          addressRegion: 'NY',
          postalCode: '<zip>',
          addressCountry: 'US'
        }
      };
    </script>
    ...
    {@html `<script type="application/ld+json">${JSON.stringify(jsonLd)}</script>`}
    ```
    CRITICAL (D-11, POOL-05): do NOT include `aggregateRating` or `review` — there are no visible on-page reviews on lipool, so a rating would trip Google's spammy-structured-data filter. The visible footer NAP block (name / address / phone, already partly present) must display the SAME name, address, and phone as the JSON-LD. Keep the existing `tel:` link. Do NOT drop the `/gallery/` or `/quote/` `rel="external"` here — 03-05 handles the gallery rel drop; quote stays (Phase 4).
  </action>
  <acceptance_criteria>
    - After `MSYS_NO_PATHCONV=1 BASE_PATH=/raj/lipool npm run build`: footer built HTML (any `build/**/index.html`) contains `application/ld+json` AND `HomeAndConstructionBusiness` AND `Nassau County` AND `Suffolk County`
    - Built HTML does NOT contain `aggregateRating` and does NOT contain `"review"` structured-data (concrete negative assertion — POOL-05)
    - Visible footer NAP shows the same phone `(516) 555-0100` as the JSON-LD `telephone`
    - Portability guard still clean (`grep -rE '(href|src)="/[^/]' build --include="*.html"` prints nothing — JSON-LD has no href/src)
    - `cd lipool && npm run check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd lipool && npm run check && MSYS_NO_PATHCONV=1 BASE_PATH=/raj/lipool npm run build && grep -q "HomeAndConstructionBusiness" build/index.html && grep -q "Nassau County" build/index.html && ! grep -q "aggregateRating" build/index.html && echo "jsonld OK"</automated>
  </verify>
  <done>Footer emits valid HomeAndConstructionBusiness JSON-LD with areaServed + NAP matching the visible block; no aggregateRating anywhere.</done>
</task>

</tasks>

<verification>
- `cd lipool && npm run check` exits 0 (QUAL-02).
- Build succeeds with `BASE_PATH=/raj/lipool` and `BASE_PATH=''`; portability grep clean.
- Home has trust strip + 6 services; /gallery/ prerenders with descriptive alt/caption per tile; tel: on every page.
- Quote CTAs carry `rel="external"` and the gallery teaser is rel-free (both asserted in Task 1 verify).
- Footer JSON-LD contains `HomeAndConstructionBusiness` + `Nassau County`/`Suffolk County` + NAP; contains NO `aggregateRating`.
</verification>

<success_criteria>
POOL-01 (services + LI trust signals), POOL-02 (gallery with descriptive alt), POOL-04 (click-to-call every page), POOL-05 (HomeAndConstructionBusiness JSON-LD + NAP, no faked aggregateRating) all met.
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-dynamic-routes/03-04-SUMMARY.md`.
</output>
