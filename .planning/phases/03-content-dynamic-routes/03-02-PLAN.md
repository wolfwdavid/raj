---
phase: 03-content-dynamic-routes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lidentist/src/lib/data/dentists.ts
  - lidentist/src/lib/components/SampleDataBanner.svelte
  - lidentist/src/lib/components/StarRating.svelte
  - lidentist/src/lib/components/DentistCard.svelte
  - lidentist/src/routes/+page.svelte
  - lidentist/src/routes/dentists/[slug]/+page.ts
  - lidentist/src/routes/dentists/[slug]/+page.svelte
autonomous: true
requirements: [DENT-01, DENT-02, DENT-03, DENT-05, DENT-06]

must_haves:
  truths:
    - "A patient browses a directory of Long Island dentists (name, practice, town, specialty, star rating + review count)"
    - "A patient filters the directory by specialty and town, and the results count is announced accessibly"
    - "A patient opens /dentists/[slug]/ with on-page review cards (stars + numeric value + text)"
    - "Every seeded dentist and review is unmistakably labeled sample data on directory AND detail pages"
    - "Ratings are aria-labeled 'X out of 5 stars, N reviews'; skip link, landmarks, 3px focus, 44px targets present"
  artifacts:
    - path: "lidentist/src/lib/data/dentists.ts"
      provides: "Typed Dentist[] (8-12) with nested reviews"
      contains: "export const dentists"
    - path: "lidentist/src/lib/components/SampleDataBanner.svelte"
      provides: "Persistent visible sample-data notice (FTC gate, DENT-05)"
      contains: "Sample data for demonstration"
    - path: "lidentist/src/routes/dentists/[slug]/+page.ts"
      provides: "entries() enumerating every dentist slug"
      contains: "export const entries"
  key_links:
    - from: "lidentist/src/routes/+page.svelte"
      to: "lidentist/src/lib/data/dentists.ts"
      via: "import { dentists } for directory + filter"
      pattern: "from '\\$lib/data/dentists'"
    - from: "lidentist/src/routes/+page.svelte"
      to: "aria-live results count"
      via: "aria-live='polite' region updated on filter"
      pattern: "aria-live=\"polite\""
---

<objective>
Give lidentist its real dentist directory: a typed `dentists.ts` (8–12 LI dentists with nested reviews), a filterable directory on the home page (specialty + town, accessible results count), per-dentist detail pages at `/dentists/[slug]/` with on-page review cards, and — the launch-blocking FTC gate — an unmistakable persistent sample-data banner on directory AND detail pages.

Purpose: Satisfies DENT-01 (directory), DENT-02 (filter), DENT-03 (detail + reviews), DENT-05 (sample-data labeling — FTC Reviews Rule, $51,744/violation), DENT-06 (a11y-exemplar bar). lidentist is the fleet a11y exemplar.
Output: dentists.ts, SampleDataBanner, StarRating, DentistCard, home directory+filter, /dentists/[slug]/ detail.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-content-dynamic-routes/03-CONTEXT.md
@.planning/design/lidentist.md
@.planning/phases/02-per-site-shells-scaffold-conventions/02-CONVENTIONS.md
@.planning/research/PITFALLS.md
@lidentist/src/routes/+page.svelte
@lidentist/src/routes/+layout.ts

<interfaces>
Prerender contract (already in lidentist/src/routes/+layout.ts — DO NOT change):
```ts
export const prerender = true;
export const trailingSlash = 'always';
```

Dentist data model (Task 1 defines; Tasks 2-3 consume):
```ts
// lidentist/src/lib/data/dentists.ts
export interface Review {
  author: string;   // 'Maria G.'
  date: string;     // ISO 'YYYY-MM-DD'
  stars: number;    // 1-5 integer
  text: string;     // 1-3 sentences
}
export interface Dentist {
  slug: string;        // kebab, unique
  name: string;        // 'Dr. Ada Nguyen'
  practice: string;    // 'Harbor Family Dental'
  town: string;        // LI town: 'Huntington' | 'Massapequa' | ...
  specialty: string;   // one of SPECIALTIES below
  rating: number;      // 0-5, one decimal, e.g. 4.8
  reviewCount: number; // total reviews (>= reviews.length)
  reviews: Review[];   // 2-4 seeded reviews
}
export const SPECIALTIES: string[]; // ['General','Pediatric','Orthodontics','Cosmetic','Oral Surgery']
export const dentists: Dentist[];
```

entries() pattern (Task 3):
```ts
// lidentist/src/routes/dentists/[slug]/+page.ts
import { error } from '@sveltejs/kit';
import { dentists } from '$lib/data/dentists';
export const entries = () => dentists.map((d) => ({ slug: d.slug }));
export function load({ params }) {
  const dentist = dentists.find((d) => d.slug === params.slug);
  if (!dentist) error(404, 'Dentist not found');
  return { dentist };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: dentists.ts data + SampleDataBanner component</name>
  <read_first>
    - The Dentist/Review model in this plan's &lt;interfaces&gt;
    - .planning/design/lidentist.md (specialties list, star ratings amber #D97706 with numeric value alongside — never color-only)
    - .planning/research/PITFALLS.md Pitfall 10 (FTC fake-review rule — sample data must be unmistakably labeled)
    - .planning/phases/03-content-dynamic-routes/03-CONTEXT.md D-05, D-06
  </read_first>
  <action>
    Create `lidentist/src/lib/data/dentists.ts` per the &lt;interfaces&gt; shape: `SPECIALTIES` = `['General','Pediatric','Orthodontics','Cosmetic','Oral Surgery']`, and `export const dentists: Dentist[]` with 8–12 records. Use real Long Island town names (Huntington, Massapequa, Garden City, Babylon, Hempstead, Smithtown, Farmingdale, Port Washington, etc.), varied specialties (cover all 5 across the set), ratings 3.9–5.0 (one decimal), reviewCount ≥ reviews.length, and 2–4 seeded reviews each (author initial-style name, ISO date, integer stars, 1–3 sentence text). Content is realistic but IS sample data (see banner). Names/towns/review text are executor's discretion.

    Create `lidentist/src/lib/components/SampleDataBanner.svelte` (D-06, FTC gate): a persistent, visually prominent `<aside>`/`<div role="note">` (NOT a footer whisper) whose visible text contains EXACTLY the string `Sample data for demonstration — not real reviews`. Style it as an unmissable banner (accent/warning treatment, adequate contrast, full-width band above content). It must be keyboard/screen-reader discoverable (real text, not background image). This component is placed on BOTH the directory and every detail page (Tasks 2 & 3).
  </action>
  <acceptance_criteria>
    - `grep -c 'slug:' lidentist/src/lib/data/dentists.ts` returns 8–12
    - File contains `export const dentists`, `export const SPECIALTIES`, `export interface Dentist`, `export interface Review`
    - `lidentist/src/lib/components/SampleDataBanner.svelte` exists and contains the exact string `Sample data for demonstration — not real reviews`
    - `cd lidentist && npm run check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd lidentist && npm run check && grep -q "Sample data for demonstration — not real reviews" src/lib/components/SampleDataBanner.svelte && echo "banner OK"</automated>
  </verify>
  <done>dentists.ts holds 8–12 typed dentists with nested reviews; SampleDataBanner renders the exact FTC label; check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Home directory + specialty/town filter + StarRating + DentistCard + banner</name>
  <read_first>
    - lidentist/src/routes/+page.svelte (existing hero with #directory section + specialty chips — extend it)
    - lidentist/src/lib/data/dentists.ts + SampleDataBanner (Task 1)
    - .planning/design/lidentist.md §"Layout Patterns" (directory grid minmax(300px,1fr); card = avatar/initials, name, practice, town, specialty tags, rating + review count) and §"Accessibility Notes" (aria-label rating, results count aria-live=polite, native select filters)
  </read_first>
  <action>
    Create `lidentist/src/lib/components/StarRating.svelte` taking `rating: number` and `reviewCount: number`: renders 5 SVG stars (filled to rating, amber `#D97706`) with a sibling numeric value (e.g. "4.8"), and an `aria-label` EXACTLY of the form `{rating} out of 5 stars, {reviewCount} reviews` (D-05/DENT-06). Never color-only — the numeric value is always visible text.

    Create `lidentist/src/lib/components/DentistCard.svelte` taking a `Dentist`: avatar (initials on accent field, decorative aria-hidden), name, practice, town, specialty tag, `<StarRating>`, and the whole card links to `{base}/dentists/{slug}/`.

    IMPORTANT (build-order guard): give that per-dentist card anchor a TEMPORARY `rel="external"` in THIS task. The `/dentists/[slug]/` route does not exist until Task 3, and this task's `<verify>` runs a full `npm run build`. DentistCard is rendered on the home directory, so the prerender crawler would follow those dangling per-dentist links → 404 → build fails (SvelteKit's default `handleHttpError` THROWS on 404 during the crawl). `rel="external"` tells the crawler not to follow the link, keeping the Task 2 build green. Task 3 (which creates the route + `entries()`) STRIPS this `rel` so the links become crawlable internal links. Keep ≥44px targets.

    Edit `lidentist/src/routes/+page.svelte`: render `<SampleDataBanner>` near the top (above the directory, persistent). Replace the placeholder `#directory` copy with a real directory: two native `<select>` filters (specialty from `SPECIALTIES` + "All"; town from the unique towns in data + "All"), client-side `$derived` filtering of `dentists`, a grid of `<DentistCard>` for the filtered set, and a results-count line in an `aria-live="polite"` region (e.g. "8 dentists"). Filters must be keyboard-operable native selects (D-07). Preserve skip-link/landmarks/one-h1 from the shell.
  </action>
  <acceptance_criteria>
    - `StarRating.svelte` contains an aria-label matching the pattern `out of 5 stars,` and ` reviews`
    - `DentistCard.svelte` links to `{base}/dentists/` + slug AND carries a TEMPORARY `rel="external"` on that per-dentist link (`grep -c 'rel="external"' lidentist/src/lib/components/DentistCard.svelte` returns ≥1) — Task 3 removes it
    - `lidentist/src/routes/+page.svelte` imports dentists, renders `<SampleDataBanner`, contains `aria-live="polite"`, and has two `<select` filter elements
    - After `MSYS_NO_PATHCONV=1 BASE_PATH=/raj/lidentist npm run build`: built home `build/index.html` contains a dentist name from data AND the string `Sample data for demonstration`
    - Built home HTML contains `out of 5 stars,` (aria-labeled ratings present)
    - `cd lidentist && npm run check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd lidentist && grep -q 'rel="external"' src/lib/components/DentistCard.svelte && npm run check && MSYS_NO_PATHCONV=1 BASE_PATH=/raj/lidentist npm run build && grep -q "Sample data for demonstration" build/index.html && grep -q "out of 5 stars," build/index.html && echo "directory OK"</automated>
  </verify>
  <done>Home shows a filterable dentist directory with accessible ratings + live results count + persistent sample-data banner; per-dentist card links are temporarily rel="external" so the build stays green before the detail route exists.</done>
</task>

<task type="auto">
  <name>Task 3: /dentists/[slug]/ detail + review cards + banner + entries(); strip temporary rel</name>
  <read_first>
    - entries() pattern in this plan's &lt;interfaces&gt;
    - lidentist/src/lib/data/dentists.ts, StarRating, SampleDataBanner (Tasks 1-2)
    - lidentist/src/lib/components/DentistCard.svelte (Task 2 — carries the temporary rel="external" you will strip)
    - .planning/design/lidentist.md §"Layout Patterns" (detail: header name/practice/specialty/town/rating, review cards = reviewer initial + date + stars + numeric + text, sticky Request-appointment CTA → /appointment/?dentist=slug)
    - .planning/phases/03-content-dynamic-routes/03-CONTEXT.md D-02, D-05, D-06
  </read_first>
  <action>
    Create `lidentist/src/routes/dentists/[slug]/+page.ts` EXACTLY per &lt;interfaces&gt;: `entries = () => dentists.map((d) => ({ slug: d.slug }))` + `load` with `error(404)` fallback (D-02 completeness).

    Create `lidentist/src/routes/dentists/[slug]/+page.svelte` consuming `data.dentist`: single `<h1>` = dentist name, header block (practice, town, specialty, `<StarRating>`), a `<SampleDataBanner>` (persistent on detail too — D-06), and on-page review cards mapping `dentist.reviews` — each card shows reviewer name, date, `<StarRating rating={r.stars} reviewCount=...>` (or per-review stars + numeric), and text (DENT-03). Include a "Request an appointment" CTA linking to `{base}/appointment/?dentist={slug}` with `rel="external"` (Phase-4 route — KEEP the guard; do NOT drop it). Strict heading hierarchy h1→h2→h3, 3px focus, 44px targets.

    Now that `/dentists/[slug]/` exists and is enumerated by `entries()`, STRIP the temporary `rel="external"` from the per-dentist anchor in `lidentist/src/lib/components/DentistCard.svelte` so the home directory links become plain crawlable internal links. NOTE: this strip is scoped to DentistCard.svelte only — the appointment CTA in THIS detail page keeps its `rel="external"` (Phase-4). This task's `<verify>` build then validates the now-live dentist links AND asserts detail page count == record count.
  </action>
  <acceptance_criteria>
    - `lidentist/src/routes/dentists/[slug]/+page.ts` contains `export const entries` and `dentists.map`
    - `lidentist/src/lib/components/DentistCard.svelte` NO LONGER contains `rel="external"` (`grep -c 'rel="external"' lidentist/src/lib/components/DentistCard.svelte` returns 0)
    - After build: `ls lidentist/build/dentists/*/index.html | wc -l` EQUALS `grep -c 'slug:' lidentist/src/lib/data/dentists.ts` (entries() completeness — INFRA-05)
    - A representative detail HTML (`build/dentists/<first-slug>/index.html`) contains review text from data, `out of 5 stars,`, and `Sample data for demonstration`
    - The appointment CTA still carries `rel="external"` in `+page.svelte` (Phase-4 route stays guarded): `grep -q 'rel="external"' lidentist/src/routes/dentists/[slug]/+page.svelte`
    - `cd lidentist && npm run check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd lidentist && ! grep -q 'rel="external"' src/lib/components/DentistCard.svelte && npm run check && MSYS_NO_PATHCONV=1 BASE_PATH=/raj/lidentist npm run build && test "$(ls build/dentists/*/index.html | wc -l)" -eq "$(grep -c 'slug:' src/lib/data/dentists.ts)" && grep -q "Sample data for demonstration" build/dentists/*/index.html && echo "detail OK"</automated>
  </verify>
  <done>Every dentist has a prerendered detail page with review cards, aria-labeled ratings, and a persistent sample-data banner; the temporary DentistCard rel="external" is stripped (appointment CTA rel kept); count == records.</done>
</task>

</tasks>

<verification>
- `cd lidentist && npm run check` exits 0 (QUAL-02; a11y-exemplar bar preserved).
- Build succeeds with `BASE_PATH=/raj/lidentist` and `BASE_PATH=''`; portability grep clean.
- DentistCard.svelte no longer carries `rel="external"` after Task 3; the appointment CTA in the detail page still does (Phase-4 guard).
- `ls build/dentists/*/index.html | wc -l` == `grep -c 'slug:' src/lib/data/dentists.ts`.
- Directory + every detail page render `Sample data for demonstration` (FTC gate) and `out of 5 stars,` (aria ratings).
- Filter results count sits in an `aria-live="polite"` region.
</verification>

<success_criteria>
DENT-01/02/03 met (directory, filter with announced count, detail + review cards); DENT-05 met (unmistakable sample-data banner on directory AND detail — FTC launch gate); DENT-06 met (aria ratings, skip link/landmarks/3px focus/44px targets); every dentist slug prerenders.
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-dynamic-routes/03-02-SUMMARY.md`.
</output>
