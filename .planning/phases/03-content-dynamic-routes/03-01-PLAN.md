---
phase: 03-content-dynamic-routes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vfamigos/src/lib/data/products.ts
  - vfamigos/src/lib/components/ProductCard.svelte
  - vfamigos/src/routes/+page.svelte
  - vfamigos/src/routes/products/+page.svelte
  - vfamigos/src/routes/products/[slug]/+page.ts
  - vfamigos/src/routes/products/[slug]/+page.svelte
  - vfamigos/src/routes/about/+page.svelte
autonomous: true
requirements: [VFAM-01, VFAM-02, VFAM-06]

must_haves:
  truths:
    - "A shopper sees a grid of original Amigo characters (name, series, price, per-character art) on the home page"
    - "A shopper opens a per-character product page at /products/[slug]/ with story/bio, traits, price, and an add-to-cart button"
    - "A shopper sees brand storytelling (hero + about) in the vfamigos design system"
    - "Every product record has its own prerendered /products/[slug]/index.html"
  artifacts:
    - path: "vfamigos/src/lib/data/products.ts"
      provides: "Typed Product[] of 8-12 Amigo characters"
      contains: "export const products"
    - path: "vfamigos/src/routes/products/[slug]/+page.ts"
      provides: "entries() enumerating every product slug for prerender"
      contains: "export const entries"
    - path: "vfamigos/src/routes/products/[slug]/+page.svelte"
      provides: "PDP: 2-col art/story, traits, price, add-to-cart"
    - path: "vfamigos/src/routes/about/+page.svelte"
      provides: "Brand storytelling route"
  key_links:
    - from: "vfamigos/src/routes/+page.svelte"
      to: "vfamigos/src/lib/data/products.ts"
      via: "import { products }"
      pattern: "from '\\$lib/data/products'"
    - from: "vfamigos/src/routes/products/[slug]/+page.ts"
      to: "products data"
      via: "entries() derived from products"
      pattern: "products.map"
---

<objective>
Give vfamigos its real product catalog: a typed `products.ts` of 8–12 original "Amigo" characters, a character grid on the home page, a `/products/` shop listing, per-character product detail pages at `/products/[slug]/` (prerendered via `entries()`), and brand-storytelling hero + `/about/` route.

Purpose: Satisfies VFAM-01 (grid), VFAM-02 (PDP), VFAM-06 (brand storytelling). Establishes the data→grid→PDP chain the Phase-4 cart will consume.
Output: products.ts, ProductCard, home grid, /products/ listing, /products/[slug]/ PDP, /about/ route.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-content-dynamic-routes/03-CONTEXT.md
@.planning/design/vfamigos.md
@.planning/phases/02-per-site-shells-scaffold-conventions/02-CONVENTIONS.md
@vfamigos/src/routes/+page.svelte
@vfamigos/src/routes/+layout.ts
@vfamigos/src/lib/components/SiteHeader.svelte

<interfaces>
<!-- Executor implements against these contracts — no codebase exploration needed. -->

Prerender contract (already in vfamigos/src/routes/+layout.ts — DO NOT change):
```ts
export const prerender = true;
export const trailingSlash = 'always';
// never ssr = false
```

New Product type (Task 1 defines this; Tasks 2-3 consume it):
```ts
// vfamigos/src/lib/data/products.ts
export interface Product {
  slug: string;      // kebab-case, unique — e.g. 'pablo-the-brave'
  name: string;      // 'Pablo the Brave'
  series: string;    // 'Season One'
  price: number;     // USD, e.g. 24
  tagline: string;   // one-line hook
  bio: string;       // 2-3 sentences of character story
  traits: string[];  // e.g. ['Brave', 'Loyal', 'Curious']
  accent: string;    // per-character hex for the art block, e.g. '#F59E0B'
}
export const products: Product[];
```

entries() pattern for the dynamic route (Task 3):
```ts
// vfamigos/src/routes/products/[slug]/+page.ts
import { error } from '@sveltejs/kit';
import { products } from '$lib/data/products';
export const entries = () => products.map((p) => ({ slug: p.slug }));
export function load({ params }) {
  const product = products.find((p) => p.slug === params.slug);
  if (!product) error(404, 'Amigo not found');
  return { product };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: products.ts data model + 8-12 Amigo records</name>
  <read_first>
    - vfamigos/src/lib/data/ (new dir) and the Product interface in this plan's &lt;interfaces&gt;
    - .planning/design/vfamigos.md §"Layout Patterns" and §"Accessibility Notes" (per-character accent, meaningful alt = name + one-line persona)
    - .planning/phases/03-content-dynamic-routes/03-CONTEXT.md D-04 (record shape, 8-12 chars, CSS/SVG art only)
  </read_first>
  <action>
    Create `vfamigos/src/lib/data/products.ts` exporting `interface Product` (exact shape from &lt;interfaces&gt;) and `export const products: Product[]` with 8–12 ORIGINAL Amigo characters (per D-04; NOT VeeFriends names/trade dress).

    Each record: unique kebab `slug`, `name`, `series` (use 2–3 series names like 'Season One'/'Season Two' across the set), integer/decimal `price` (USD, e.g. 18–48), one-line `tagline`, 2–3 sentence `bio` (collectible storytelling — the personality/backstory), 3–5 `traits`, and a per-character `accent` hex (distinct, WCAG-safe as a background behind white text or used only as a solid art field — text itself always sits on white/near-white per design spec).

    Imagery is CSS/SVG-generated only (D-01) — the `accent` hex + the character's monogram (first letter of name) is the art. NO binary image assets, NO external image URLs.

    Record count is executor's discretion within 8–12; names/bios/traits are your discretion but must read as a believable original character brand.
  </action>
  <acceptance_criteria>
    - `vfamigos/src/lib/data/products.ts` exists and `grep -c 'slug:' vfamigos/src/lib/data/products.ts` returns a number between 8 and 12
    - File contains `export const products` and `export interface Product`
    - `grep -E 'accent:' vfamigos/src/lib/data/products.ts` shows a per-record hex (one per product)
    - No `http` image URL and no `.png`/`.jpg`/`.webp` import in the file
    - `cd vfamigos && npm run check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd vfamigos && npm run check</automated>
  </verify>
  <done>products.ts exports a typed Product[] of 8–12 Amigos with per-character accent + traits + bio; svelte-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: ProductCard + home grid + /products/ listing + /about/ route</name>
  <read_first>
    - vfamigos/src/routes/+page.svelte (existing hero — extend, keep the hero + its blob)
    - vfamigos/src/lib/data/products.ts (Task 1)
    - .planning/design/vfamigos.md §"Layout Patterns" (grid: repeat(auto-fill, minmax(240px, 1fr)); card = accent art block + name + series tag + price)
    - .planning/phases/02-per-site-shells-scaffold-conventions/02-CONVENTIONS.md §5 (nav rule — internal links to routes built in THIS plan get NO rel)
  </read_first>
  <action>
    Create `vfamigos/src/lib/components/ProductCard.svelte` — a reusable card taking a `Product` prop. Renders the character art block (solid `accent` background with the name's monogram SVG/large initial, `aria-hidden` on the decorative art), name, series tag, price, and wraps the whole card in an anchor to `{base}/products/{slug}/` (internal, NO rel="external" — the route is built in Task 3 this same plan and covered by entries()). Card art block gives the character a meaningful accessible name via visible text (name) — decorative monogram is aria-hidden. Hover scale 1.02 per design motion.

    Edit `vfamigos/src/routes/+page.svelte`: keep the existing hero; ADD below it (a) a product grid section `id="products"` mapping `products` to `<ProductCard>` (grid `repeat(auto-fill, minmax(240px, 1fr))`), and (b) an About teaser section `id="about"` with 1–2 short paragraphs of brand story + a link to `{base}/about/` (internal, NO rel — /about/ built in this task). This satisfies VFAM-01 (grid on home) and part of VFAM-06 (hero + about).

    Create `vfamigos/src/routes/products/+page.svelte` — the Shop listing: `<h1>Shop the Amigos</h1>` + the full grid of `<ProductCard>` over `products`. (This is the target of the nav "Shop" link; it is a static route, auto-prerendered.)

    Create `vfamigos/src/routes/about/+page.svelte` — brand storytelling page: single `<h1>`, 2–4 paragraphs of the vfamigos origin/collection story per design spec, styled in the vfamigos system. Static route (auto-prerendered). Completes VFAM-06.

    Do NOT touch SiteHeader/SiteFooter rel="external" here — 03-05 sweeps those after all Wave-1 routes exist.
  </action>
  <acceptance_criteria>
    - `vfamigos/src/lib/components/ProductCard.svelte` exists and links to `{base}/products/` + slug (grep `products/` in the file)
    - `vfamigos/src/routes/+page.svelte` imports products and contains `id="products"` and `id="about"`
    - `vfamigos/src/routes/products/+page.svelte` and `vfamigos/src/routes/about/+page.svelte` both exist with exactly one `<h1>`
    - Build emits static routes: after `MSYS_NO_PATHCONV=1 BASE_PATH=/raj/vfamigos npm run build`, `vfamigos/build/products/index.html` and `vfamigos/build/about/index.html` both exist
    - Built home HTML contains a product name from products.ts (grep the home `build/index.html` for one known character name)
    - `cd vfamigos && npm run check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd vfamigos && npm run check && MSYS_NO_PATHCONV=1 BASE_PATH=/raj/vfamigos npm run build && ls build/products/index.html build/about/index.html</automated>
  </verify>
  <done>Home shows the Amigo grid + about teaser; /products/ listing and /about/ static routes render real content and prerender.</done>
</task>

<task type="auto">
  <name>Task 3: /products/[slug]/ PDP with entries() + inert add-to-cart</name>
  <read_first>
    - The entries() pattern in this plan's &lt;interfaces&gt;
    - vfamigos/src/lib/data/products.ts (Task 1)
    - .planning/design/vfamigos.md §"Layout Patterns" (PDP = 2-col art/story panel + traits list + price + add-to-cart/buy button)
    - .planning/phases/03-content-dynamic-routes/03-CONTEXT.md D-02 (entries() completeness — build file count == record count) and D-04 (add-to-cart button present but inert/disabled-noted this phase; cart logic Phase 4)
  </read_first>
  <action>
    Create `vfamigos/src/routes/products/[slug]/+page.ts` EXACTLY per the &lt;interfaces&gt; pattern: `export const entries = () => products.map((p) => ({ slug: p.slug }))` plus a `load({ params })` that finds the product and `error(404)` if missing. This is the D-02 prerender guarantee — every slug gets a page.

    Create `vfamigos/src/routes/products/[slug]/+page.svelte` consuming `data.product`: 2-col desktop layout (character art block using `product.accent` + monogram on the left; story/buy panel on the right), single `<h1>` = product name, series tag, `product.bio`, a traits list (`product.traits` rendered as badges), price, and an add-to-cart `<button>` that is PRESENT but INERT this phase — `disabled` with a visible note like "Cart coming soon" (D-04; cart wiring is Phase 4). The button must be a real `<button>` (≥44px), not a link, and must NOT fabricate any success state. Collapses to 1-col on mobile.

    Meaningful alt/label: the art block is decorative (aria-hidden); the character is named by the `<h1>`.
  </action>
  <acceptance_criteria>
    - `vfamigos/src/routes/products/[slug]/+page.ts` contains `export const entries` and `products.map`
    - After `MSYS_NO_PATHCONV=1 BASE_PATH=/raj/vfamigos npm run build`: `ls vfamigos/build/products/*/index.html | wc -l` EQUALS `grep -c 'slug:' vfamigos/src/lib/data/products.ts` (entries() completeness — INFRA-05)
    - A representative PDP built HTML (e.g. `build/products/<first-slug>/index.html`) contains the character's bio text and the string `disabled` on the add-to-cart button
    - PDP add-to-cart is a `<button ... disabled>` — grep confirms no `href` on it and no "success"/"added to cart" confirmation text
    - `cd vfamigos && npm run check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd vfamigos && npm run check && MSYS_NO_PATHCONV=1 BASE_PATH=/raj/vfamigos npm run build && test "$(ls build/products/*/index.html | wc -l)" -eq "$(grep -c 'slug:' src/lib/data/products.ts)" && echo "count OK"</automated>
  </verify>
  <done>Every Amigo has a prerendered PDP with story/traits/price and an inert add-to-cart; built page count == record count.</done>
</task>

</tasks>

<verification>
- `cd vfamigos && npm run check` exits 0 (QUAL-02 preserved).
- `MSYS_NO_PATHCONV=1 BASE_PATH=/raj/vfamigos npm run build` succeeds; `BASE_PATH='' npm run build` also succeeds (portability).
- `ls build/products/*/index.html | wc -l` == `grep -c 'slug:' src/lib/data/products.ts`.
- `grep -rE '(href|src)="/[^/]' build --include="*.html"` prints nothing (portability guard).
- Built home/PDP HTML contains real character names + bios (no empty shell).
</verification>

<success_criteria>
VFAM-01 (home grid), VFAM-02 (per-character PDP with story/traits/price/add-to-cart), and VFAM-06 (hero + about storytelling) are met; every product slug prerenders its own page; no fake cart success state.
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-dynamic-routes/03-01-SUMMARY.md`.
</output>
